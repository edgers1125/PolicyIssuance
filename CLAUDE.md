# PolicyIssuance

Insurance policy/quotation issuance app for Bethel General Insurance and Surety Corp.

## Stack

- **backend/**: Express 5 + Prisma 7 (PostgreSQL, `@prisma/adapter-pg`) + JWT auth (`jsonwebtoken`) + `zod` v4 validation. CommonJS (`require`).
- **frontend/**: React 19 + Vite + MUI 9 (`@mui/material`, `@mui/icons-material`). No Tailwind, no shadcn. React Router 7.
- No tRPC, no shared package — frontend calls backend REST via a hand-written `fetch` wrapper (`frontend/src/api/client.js`).

## Dev environment — Docker Compose

`docker-compose.yml` at repo root runs 4 services: `db` (postgres:16, port 5432), `backend` (port 4000), `frontend` (port 5173), `prisma-studio` (port 5555). Container names: `policyissuance-backend-1`, `policyissuance-frontend-1`, `policyissuance-db-1`, `policyissuance-prisma-studio-1`.

**Gotcha**: `backend`/`frontend`/`prisma-studio` node_modules are named Docker volumes, *not* bind-mounted from the host. Running `npm install <pkg>` on the host updates `package.json`/`package-lock.json` (bind-mounted) but **does not** reach the container's node_modules. After adding a dependency, restart the container to re-run its `npm install && npx prisma generate && npm run dev` start command:
```
docker restart policyissuance-backend-1
```
Source is bind-mounted with `CHOKIDAR_USEPOLLING=true` (Windows host, nodemon/Vite poll instead of relying on inotify).

`.claude/launch.json` points `preview_start` at `npm --prefix backend/frontend run dev` — if the Docker containers are already up on those ports, `preview_start` fails with "port in use"; just `navigate` to `http://localhost:5173` directly instead of starting a second copy.

**Local test login**: seed script (`backend/prisma/seed.js`) creates `admin@policyissuance.local` / `Password123!`, but the dev DB may have different data/passwords already. Check first: `docker exec policyissuance-db-1 psql -U policyapp -d policyissuance -c "select id,email,agent_id from \"User\";"`. To test API/UI without knowing a password, mint a JWT directly with the `JWT_SECRET` from `backend/.env` (payload shape `{ userId, email }`, see `backend/src/utils/jwt.js`) and inject it into the browser's `localStorage` (`token` + `user` keys, see `AuthContext.jsx`).

## Validation contract — read before touching any route

**Every backend route's `body`/`query`/`params` is validated by a zod schema via `validateBody`/`validateQuery`/`validateParams` (`backend/src/middleware/validate.js`).** Convention: **one schema file per route file, same name** (`routes/policyQuotations.js` ↔ `schemas/policyQuotations.js`), imported directly into the route. Shared cross-route building blocks live in `schemas/common.js` (`requiredString`, `requiredEmail`) and `schemas/policyIntakeShared.js` (address/vehicle/coverage input shapes + refinements shared by applications and quotations).

**Rule: any time a route's accepted input changes (new field, renamed field, changed type, new endpoint, new required-ness), its zod schema must be updated in the same change — never let a route accept something its schema doesn't describe, and never leave a schema describing a field the route no longer reads.** This includes new `:id`/query params (add a param schema, validate with `validateParams`/`validateQuery`) and new refinements when a field's validity now depends on another field. Zod v4 syntax is used throughout (`z.coerce.date({ error: "..." })`, not `required_error`).

---

## Backend (`backend/src/`)

### Route mount table (from `src/index.js`)

| Router file | Mount path |
|---|---|
| `routes/auth.js` | `/auth` |
| `routes/users.js` | `/users` |
| `routes/customers.js` | `/customers` |
| `routes/companies.js` | `/companies` |
| `routes/agents.js` | `/agents` |
| `routes/vehicles.js` | `/vehicles` |
| `routes/addresses.js` | `/addresses` |
| `routes/catalog.js` | `/` (root) |
| `routes/coveragePricing.js` | `/` (root) |
| `routes/policyApplications.js` | `/policy-applications` |
| `routes/policyQuotations.js` | `/policy-quotations` |
| `routes/policyApproval.js` | `/policy-approval` |
| `routes/policies.js` | `/policies` |
| `routes/paymentMethods.js` | `/payment-methods` |

Plus **`GET /me`** defined inline in `index.js` (`requireAuth` only) — returns the current user + `agent` info + `permissions: Array.from(getUserPermissionCodes(userId))`. `catalog.js`/`coveragePricing.js` deliberately apply `requireAuth`/permission checks *per-route* rather than via `router.use()`, since both mount at `/` alongside unrelated routes like `/me`.

### Endpoint inventory

**`routes/addresses.js`** — router-level `requireAuth, requirePermission("CREATE_APPLICATION")`
| Method & path | Schema | Notes |
|---|---|---|
| `PATCH /:id` | `updateAddressSchema` | Requires `agentCanEditAddress` (ownership via `PartyAddress` → `CustomerAgent`/`CompanyAgent`). `country` defaults `"Philippines"`. |

**`routes/agents.js`** — router-level `requireAuth, requirePermission("MANAGE_AGENTS")`
| Method & path | Schema | Notes |
|---|---|---|
| `GET /` | — | Lists agents; `premiums_generated(_30d)` needs `MANAGE_AGENTS.VIEW_AGENT_PREMIUMS`, `special_rates` needs `MANAGE_AGENTS.MANAGE_AGENT_RATES` (field-gated via `ensurePermission`). Revenue = sum of `ApplicationCoverage.payable_to_bethel` grouped by agent — frozen historical figures. |
| `GET /:id/netrates` | `getAgentRatesQuerySchema` | `MANAGE_AGENTS.MANAGE_AGENT_RATES`. Coverages at one `coverage_in_days`, with agent overrides layered on product defaults. |
| `PUT /:id/netrates` | `updateNetratesSchema` | Replace-all `AgentNetrate` rows for one period (transactional deleteMany+createMany). |
| `PUT /:id/value-percentage-tiers/:coverageId` | `updateAgentValueTiersSchema` | Replace-all `AgentValuePercentageTier`; rejects duplicate `min_value`. |
| `PUT /:id/flat-tiers/:coverageId` | `updateAgentFlatTiersSchema` | Replace-all `AgentFlatTierPricing`; rejects duplicate `coverage_amount`. |

**`routes/auth.js`** — no router-level auth (pre-login)
| Method & path | Schema | Notes |
|---|---|---|
| `POST /login` | `loginSchema` | bcrypt compare, issues JWT. |
| `POST /forgot-password` | `forgotPasswordSchema` | Generates reset token (1h TTL), emails the reset link via `lib/mailer.js` (also logged to console as a fallback); a send failure is caught and swallowed rather than surfaced, since this route always returns the same generic message regardless of outcome (anti-enumeration — a differing response on mail failure would itself leak whether the account exists). |
| `POST /set-password` | `setPasswordSchema` | Consumes `invite_token`, sets password, `status: ACTIVE`. |

**`routes/catalog.js`** — mounted at `/`, per-route auth
| Method & path | Schema | Notes |
|---|---|---|
| `GET /product-catalog` | — | `CREATE_APPLICATION`. Full catalog annotated with the caller's agent netrate/tier overrides — feeds the application/quotation intake form. Each allowable period also carries `has_pricing` (and `rate: null` rather than a fake `0`, when unconfigured) — a period can exist before anyone's actually priced it under Settings → Coverage Pricing, and the intake forms use this to disable selecting a coverage that has nothing to price it with yet, rather than letting it through to a confusing rejection at submit time. |
| `GET /coverages` | — | `requireAuth` + (`MANAGE_SETTINGS.EDIT_CLAUSES` OR `MANAGE_SETTINGS.MANAGE_COVERAGE_PRICING`). |
| `PATCH /coverages/:id` | `updateCoverageSchema` | `clause` needs `EDIT_CLAUSES`, `maximum_coverage` needs `MANAGE_COVERAGE_PRICING` (field-gated). |
| `GET /product-variants` | — | `MANAGE_SETTINGS.MANAGE_COVERAGE_PRICING`. Flat list of every active `ProductVariant` (`id`, `variant_code`, `variant_name`, `class_name`, `deductible_rate`, `authorized_repair_limit_rate`) — Settings → Vehicle Rates' picker, same "flat list for a settings page" pattern as `GET /coverages`. |
| `PATCH /product-variants/:id` | `productVariantIdParamSchema` + `updateProductVariantRatesSchema` | `MANAGE_SETTINGS.MANAGE_COVERAGE_PRICING`. Sets/clears `deductible_rate`/`authorized_repair_limit_rate` — the rates `pdf/theme.js`'s `computeDeductibleFigures()` uses to print a Motor policy schedule's Section III Deductible/Authorized Repair Limit line (see "PDF generation" below). Either field can be set independently or cleared back to `null` (sent explicitly, since both are optional-and-nullable). |

**`routes/companies.js`** — router-level `requireAuth` only; each route below applies `requirePermission` individually (same reasoning as `catalog.js`/`coveragePricing.js` — `GET /agent/:agentId` needs a different grant an existing-route caller may not hold)
| Method & path | Schema | Notes |
|---|---|---|
| `GET /` | — | `CREATE_APPLICATION`. Companies linked to caller's agent via `CompanyAgent`, with active vehicles/addresses. |
| `POST /` | `companyInputSchema` | `CREATE_APPLICATION`. Creates + links via `CompanyAgent`. 409 on duplicate `company_code`/`email`. |
| `PATCH /:id` | `companyInputSchema` | `CREATE_APPLICATION`. 403 if not linked to this agent. |
| `GET /agent/:agentId` | `agentIdParamSchema` | `QUOTATION_TRACKER.ADMIN_CREATE_QUOTATION`. Same shape as `GET /` above, scoped to `:agentId` instead of the caller's own — the New Quotation form's cross-agent company picker (`routes/policyQuotations.js`'s `POST /` `agent_id` override needs somewhere to pick a party from once a different agent is chosen). |

**`routes/coveragePricing.js`** — mounted at `/`, each route applies `[requireAuth, requirePermission("MANAGE_SETTINGS.MANAGE_COVERAGE_PRICING")]` individually
| Method & path | Schema | Notes |
|---|---|---|
| `POST /coverages/:id/allowable-periods` | `createAllowablePeriodSchema` | New `CoverageAllowablePeriod`; 400 if it already exists. |
| `GET /coverages/:id/pricing` | `getPricingQuerySchema` | Returns mode + rate/tiers for one (coverage, period). |
| `PATCH /coverages/:id/pricing` | `updatePricingModeSchema` | Sets `pricing_mode`, upserts `standard_rate` (PERCENTAGE only). |
| `PUT /coverages/:id/value-percentage-tiers` | `updateValuePercentageTiersSchema` | Replace-all; rejects duplicate `min_value`. |
| `PUT /coverages/:id/flat-tiers` | `updateFlatTiersSchema` | Replace-all; rejects duplicate `coverage_amount`. |

**`routes/customers.js`** — router-level `requireAuth` only; each route below applies `requirePermission` individually (same reasoning as `companies.js` above)
| Method & path | Schema | Notes |
|---|---|---|
| `GET /` | — | `CREATE_APPLICATION`. Customers linked to caller's agent via `CustomerAgent`. |
| `POST /` | `customerInputSchema` | `CREATE_APPLICATION`. 409 on duplicate email. |
| `PATCH /:id` | `customerInputSchema` | `CREATE_APPLICATION`. 403 if not linked to this agent. |
| `GET /agent/:agentId` | `agentIdParamSchema` | `QUOTATION_TRACKER.ADMIN_CREATE_QUOTATION`. Same shape as `GET /` above, scoped to `:agentId` instead of the caller's own — the New Quotation form's cross-agent customer picker, mirroring `companies.js`'s own `GET /agent/:agentId`. |

**`routes/paymentMethods.js`** — router-level `requireAuth` only
| Method & path | Schema | Notes |
|---|---|---|
| `GET /` | — | Any authenticated user (needed to fill out an application). |
| `POST /` | `createPaymentMethodSchema` | `MANAGE_SETTINGS.MANAGE_PAYMENT_METHODS`. 409 on duplicate name. |
| `DELETE /:id` | — | Same permission. 409 if referenced by any `PolicyApplication.bethel_payment_method_id`. |

**`routes/policyApplications.js`** — router-level `requireAuth, requirePermission("CREATE_APPLICATION")`
| Method & path | Schema | Notes |
|---|---|---|
| `GET /` | `listApplicationsQuerySchema` | Paginated, own-agent-only, newest first (Policy Applications tracker list) — same shape/pattern as `GET /policy-quotations`. Each row carries `policy_type` (the tracker's own "Type" column, `NEW_POLICY`/`RENEWAL`). |
| `POST /` | `createApplicationSchema` | Also requires `CREATE_APPLICATION.AGENT_ISSUANCE` in-handler. Full intake → pricing → transactional write. See "Shared application/quotation intake logic" below. If `send_policy_to_email` was checked, sends the "now under approval, please settle payment" email (see `lib/applicationEmails.js`) right after the transaction commits — never inside it, and a send failure is caught and logged rather than failing the create (same swallow-and-log pattern as `users.js`'s invite email). The optional `renewed_policy_id` field (shape-only in the schema — a valid UUID) is what the Client Policies page's "Renew This Policy" flow sends — when present, the route fetches that `Policy`, 403s if it isn't on file for the caller's own agent, 400s if `coverage_start_at` precedes its `expiry_date`, and sets the created row's `policy_type: "RENEWAL"` + `renewed_policy_id` accordingly; omitted (every other entry point — a fresh filing, or `POST /policy-quotations/:id/submit`, which never sends this field at all), `policy_type` is `NEW_POLICY` (the column's own DB default, applied by simple omission from the create call rather than set explicitly). |
| `POST /preview-pdf` | `documentPreviewPropsSchema` | Read-only — renders a PDF from live, not-yet-saved draft data via `pdf/policyApplicationPdf.js`. `PolicyApplication.jsx`'s "Print / Save as PDF" button. Returns `application/pdf`, not JSON. |
| `GET /:id` | `applicationIdParamSchema` | Full detail (Policy Applications tracker's row detail popup); 404 unless owned by caller's agent. Any recorded `PolicyApplicationChange` rows are folded in via `applyChangesToDetail()` (see below) so an agent always sees the corrected picture — the same folding `routes/policyApproval.js` applies for an approver. |
| `GET /:id/changes` | `applicationIdParamSchema` | Every `PolicyApplicationChange` recorded against this application, oldest first — the tracker detail popup's read-only "Recorded Changes" list, so an agent can see what an approver corrected on their filing. 404 unless owned by caller's agent. Same response shape as `GET /policy-approval/:id/changes`, just agent-scoped. |
| `GET /:id/pdf` | `applicationIdParamSchema` | PDF download for an already-saved application via `pdf/policyApplicationPdf.js` — the tracker detail popup's "Re-export PDF" action. Any recorded changes are folded in first (`applyChangesToDetail()`) so the export reflects the latest corrections. Always rendered with `isPreview: true` (an application isn't an issued `Policy` yet, whatever its own `status`, so the exported document keeps saying so) — see `Policy.application_id` above for when it actually becomes one. Returns `application/pdf`, not JSON. |
| `POST /:id/resend-email` | `applicationIdParamSchema` | Emails the insured party via `lib/mailer.js`, with the same (change-folded) PDF attached; 400 if no email on file; 503 if SMTP unconfigured. Mirrors `POST /policy-quotations/:id/resend-email`. |

There is deliberately no `PATCH /:id` (or any other write route) here beyond creation — once submitted, a `PolicyApplication` is immutable through this router; see `routes/policyApproval.js` below for how a correction actually reaches one, without ever writing to this table (`PolicyApplicationChange` instead — see the Database section below). `applicationDetailSelect`/`toApplicationDetail`/`toPreviewProps`/`applyChangesToDetail` are attached onto the exported `router` function (not duplicated) so `policyApproval.js` can reuse the exact same detail shape/mapping/change-folding for its own non-agent-scoped routes. `applicationDetailSelect`'s `vehicles`/`coverages` selects also carry each row's own join-table `id` (`application_vehicle_id` on the mapped vehicle, `id` on the mapped coverage) — not needed by this router's own consumers, but `policyApproval.js`'s change form needs to address "this application's 2nd vehicle" or "this specific coverage line" unambiguously. `applyChangesToDetail(detail, changes)` — not a route — folds recorded changes onto a `toApplicationDetail()` result for the change types that never mutate a `Vehicle`/`Address` row (`INSURED_FROM_DATE`, `INSURED_NAME_DETAILS`, `ADD_CLAUSE`/`REMOVE_CLAUSE`) and so wouldn't otherwise show up in a fresh read; `VEHICLE_*`/`INSURED_ADDRESS_DETAILS` changes need no entry here since they're already live on the rows `toApplicationDetail()` read fresh. `INSURED_FROM_DATE` shifts `coverage_end_at` too, by the same amount as `coverage_start_at` — the period's length (`coverage_end_at - coverage_start_at`, captured from the original detail before any change is applied) is fixed at whatever it was quoted/applied for, so correcting the *from* date moves the whole period rather than just pushing the start out and silently shrinking/stretching it. For `INSURED_NAME_DETAILS`, alongside overwriting `insured_name` with `change.change_to`, it also re-derives `insured_first_name`/`insured_middle_name`/`insured_last_name` by splitting that same "Last, First Middle" string back apart, so a second name correction prefills its form from the last correction, not the stale original `Customer` row. Originally lived only in `routes/policyApproval.js` (approver-only); moved here so both routers — an agent viewing their own filing and an approver reviewing anyone's — see the same corrected picture wherever an application's detail/PDF/emailed copy is rendered.

`formatInsuredName({ first_name, middle_name, last_name })`/`formatAddress(address)` — also not routes, also attached onto `router` — the single-source-of-truth "Last, First Middle" / comma-joined-address-line formatters used everywhere a `Customer`'s name or an `Address`'s fields need to become one display/snapshot string: `toApplicationDetail()`'s `insured_name`/`insured_address` here, `POST /policy-approval/:id/changes`'s `change_from` computation, and `POST /policy-approval/:id/approve`'s default `customerNameSnapshot`. Exist specifically so `middle_name` (previously dropped by three separately hand-rolled `[last, first].join(", ")` calls) and an address's `postal_code`/`country` (previously dropped from the combined string and from `applicationDetailSelect` entirely) stay included everywhere consistently, instead of each call site silently disagreeing. `toApplicationDetail()` also exposes the split fields themselves — `insured_first_name`/`insured_middle_name`/`insured_last_name` and `insured_address_line_1`/`insured_address_line_2`/`insured_barangay`/`insured_city`/`insured_province`/`insured_postal_code`/`insured_country` — purely so `routes/policyApproval.js`'s "Create Change" form can prefill separate inputs per field instead of asking an approver to retype the whole combined string.

**`routes/policyQuotations.js`** — router-level `requireAuth, requirePermission("QUOTATION_TRACKER")`. `QUOTATION_TRACKER` is page access only; every route below needs its own more specific grant on top of it. Four sub-permissions, each with a base (own-agent) and `ADMIN_*` (any-agent) tier: `QUOTATION_TRACKER.VIEW_QUOTATION`/`QUOTATION_TRACKER.ADMIN_VIEW_QUOTATION` gate every read below, `QUOTATION_TRACKER.CREATE_QUOTATION`/`QUOTATION_TRACKER.ADMIN_CREATE_QUOTATION` gate every write. Two local helpers resolve which tier applies and do the actual gating (writing the 403/400 response and returning `null` on failure — every route below does `if (!scope) return;`/`if (!agent) return;` right after calling one): `resolveScope(req, res, baseCode, adminCode, actingPermissions?)` — for an action against quotations in general or one already-existing quotation — returns `{ agentId: null }` (every agent) once `adminCode` is held, else `{ agentId: <caller's own> }` once `baseCode` is held (400 if the caller has no agent link), else 403; and `resolveWriteAgent(req, res, actingPermissions)` — for `POST /` specifically, which is choosing an agent for a *new* row rather than checking one already on an existing row — resolves `req.body.agent_id` (403 unless `ADMIN_CREATE_QUOTATION` is held) or falls back to the caller's own agent, returning the resolved `Agent` record. `ensureAnyPermission(res, actingPermissions, codes)` covers the "base tier OR admin tier" check `ensurePermission` (middleware/permissions.js, single-code only) can't express.
| Method & path | Schema | Notes |
|---|---|---|
| `GET /agents` | — | `QUOTATION_TRACKER.ADMIN_CREATE_QUOTATION` (extra route-level grant, on top of router-level `QUOTATION_TRACKER`). Every `ACTIVE` agent (`id`/`agent_code`/`agent_name` only) — the New Quotation form's "File under agent" picker. Deliberately its own minimal route rather than reusing `GET /agents` (`routes/agents.js`), which needs `MANAGE_AGENTS` — a different permission an admin quotation-creator may not hold — and returns premium/rate data this picker has no business seeing. |
| `GET /` | `listQuotationsQuerySchema` | `resolveScope(VIEW_QUOTATION, ADMIN_VIEW_QUOTATION)` — own-agent-only unless `ADMIN_VIEW_QUOTATION` is held, in which case every agent's quotations are returned and each row carries `agent_code`/`agent_name` (harmless when scoped to one agent — `Quotations.jsx` only renders the Agent column when it actually has `ADMIN_VIEW_QUOTATION`). Paginated, newest first (Quotation Tracker list). Each row's `status` is never stored — derived as `"FOR_ISSUANCE"` (with `converted_application_id`/`converted_application_number`) or `"SUBMITTED"`, purely from whether `converted_application` is set. `Quotations.jsx`'s Status column renders `FOR_ISSUANCE` as a single clickable `Chip` (`For Issuance · <application_number>`, an arrow icon, linking to `/policy-application?open=<converted_application_id>`) rather than a chip plus a separate plain-text link, so the status and the application it became read as one navigable unit — `PolicyApplications.jsx` reads that query param on mount to auto-open the application's own detail popup. |
| `POST /` | `createQuotationSchema` | `ensureAnyPermission([CREATE_QUOTATION, ADMIN_CREATE_QUOTATION])` then `resolveWriteAgent()` — the resolved `Agent` (not necessarily the caller's own — see `agent_id` below) is used throughout: party-ownership checks, `resolveCoverageRows`'s `agentId` (net rate/tier lookup), and the created row's `agent_id`/`agent_name_snapshot`. Same intake/pricing pattern as applications, minus payment fields. |
| `POST /preview-pdf` | `documentPreviewPropsSchema` | `ensureAnyPermission([CREATE_QUOTATION, ADMIN_CREATE_QUOTATION])`. Read-only — renders a PDF from live, not-yet-saved draft data via `pdf/quotationPdf.js`. `QuotationCreator.jsx`'s "Print / Save as PDF" button. Returns `application/pdf`, not JSON. |
| `GET /:id` | `quotationIdParamSchema` | `resolveScope(VIEW_QUOTATION, ADMIN_VIEW_QUOTATION)`. Full detail (Quotation Tracker popup); 404 unless owned by caller's agent (or, with `ADMIN_VIEW_QUOTATION`, any agent). |
| `PATCH /:id` | `quotationIdParamSchema` + `updateQuotationSchema` | `resolveScope(CREATE_QUOTATION, ADMIN_CREATE_QUOTATION)` — editing is create-tier, not view-tier. Quotation Tracker's Actions-column edit action — coverage period, `send_policy_to_email`, and coverages only (party/product/vehicles/addresses are fixed at creation); re-prices via `resolveCoverageRows`, against the quotation's *own* `agent_id` (not the caller's — an `ADMIN_CREATE_QUOTATION` caller editing someone else's quotation must still re-price at that agent's own net rates), same as `POST /`. 409 if already converted (see `POST /:id/submit`) — this and that route together are what make a converted quotation immutable. |
| `GET /:id/pdf` | `quotationIdParamSchema` | `resolveScope(VIEW_QUOTATION, ADMIN_VIEW_QUOTATION)`. PDF download for an already-saved quotation via `pdf/quotationPdf.js` — the Quotation Tracker detail popup's "Re-export PDF" action. Returns `application/pdf`, not JSON. |
| `POST /:id/resend-email` | `quotationIdParamSchema` | `resolveScope(VIEW_QUOTATION, ADMIN_VIEW_QUOTATION)` — resending is a view-tier action, not create-tier. Emails the insured party via `lib/mailer.js`, with the same PDF attached; 400 if no email on file; 503 if SMTP unconfigured. |
| `POST /:id/submit` | `quotationIdParamSchema` + `submitQuotationSchema` | `ensurePermission("CREATE_APPLICATION.AGENT_ISSUANCE")` (independent of, and in addition to, the check below — converting into an application is "issuing" one, same grant `POST /policy-applications` needs) then `resolveScope(CREATE_QUOTATION, ADMIN_CREATE_QUOTATION)` to locate the quotation itself. Quotation Tracker's Actions-column submit action — converts the quotation into a `PolicyApplication`. Carries over party/vehicles/addresses/coverages exactly as quoted (never re-priced); `submitQuotationSchema` supplies what a quotation never collects: `payment_method`, `payment_remittance`, `bethel_payment_method_id?`, and both application-only delivery flags — `send_policy_to_email?` and `send_policy_to_email_on_approval?` — fresh, rather than carrying `send_policy_to_email` over from the quotation's own stored value ("email the client the quotation" and "email the client that their application is now under approval" are different decisions the agent makes at different times). 409 if already converted, or per `lib/policyConflicts.js` (see its own entry under Lib below) if one of the quotation's vehicles (Motor) or its risk address (Property) already has a pending-approval application, or an active policy whose effectivity period overlaps this quotation's coverage period. Same post-commit "now under approval" email as `POST /policy-applications` above, when `send_policy_to_email` is set. |

**`routes/policyApproval.js`** — router-level `requireAuth, requirePermission("APPROVE_APPLICATION")`, mounted at `/policy-approval`
| Method & path | Schema | Notes |
|---|---|---|
| `GET /` | `listAllApplicationsQuerySchema` | Paginated, newest first, **every application in the system regardless of agent** (each row carries `agent_code`/`agent_name`) — the opposite scoping of `GET /policy-applications`. Backs the Policy Approval page's table. |
| `GET /:id` | `applicationIdParamSchema` | Full JSON detail, not scoped to any agent — same `applicationDetailSelect`/`toApplicationDetail` as `GET /policy-applications/:id`, with any recorded `PolicyApplicationChange` rows folded on top via `applyChangesToDetail()` (imported from `routes/policyApplications.js` — see below) so an approver always sees the corrected picture. Backs the approval dialog's change form (needs vehicle/coverage ids to reference) and its change list. |
| `GET /:id/pdf` | `applicationIdParamSchema` (reused from `schemas/policyApplications.js`) | PDF for one application, not scoped to any agent — reuses `applicationDetailSelect`/`toApplicationDetail`/`toPreviewProps`/`applyChangesToDetail` from `routes/policyApplications.js` and the same `buildPolicyApplicationPdf`, with changes folded in first so the exported PDF always reflects the latest recorded corrections, even before approval. No resend-email route alongside it: an approver reviewing someone else's application has no business emailing the customer on the agent's behalf. |
| `GET /:id/changes` | `applicationIdParamSchema` | Every `PolicyApplicationChange` recorded against this application, oldest first — the approval dialog's change-history list. |
| `POST /:id/changes` | `applicationIdParamSchema` + `createApplicationChangeSchema` | Records one correction (the approval dialog's "Create Change" action) — see `PolicyApplicationChange` below. Never touches the `PolicyApplication` row; 409 once the application is `APPROVED`. `change_from` is always computed here, never trusted from the client; `change_to` is `formatInsuredName()`/`formatAddress()` (both imported from `routes/policyApplications.js`) over the client-sent structured fields for `INSURED_NAME_DETAILS`/`INSURED_ADDRESS_DETAILS`, or `new_value` verbatim for every other type. `INSURED_ADDRESS_DETAILS` writes every field of `new_address` onto the underlying `Address` row (`tx.address.update({ data: { ...new_address } })`), not just `address_line_1`. |
| `POST /:id/approve` | `applicationIdParamSchema` + `approveApplicationSchema` | Approves the application — issues the actual `Policy` (its own independent `PolicyVehicle`/`PolicyAddress`/`PolicyCoverage` rows, per the three-parallel-entities pattern below), sets `PolicyApplication.status = APPROVED`, and logs an `ApprovalHistory` row (`decision: APPROVED`). 409 if already `APPROVED`. `VEHICLE_*`/`INSURED_ADDRESS_DETAILS` changes need no special handling — the `Vehicle`/`Address` rows read here are already corrected; `INSURED_FROM_DATE`/`INSURED_NAME_DETAILS`/clause changes are folded in explicitly (`Policy.effective_date`, `*_name_snapshot`, `PolicyCoverage.clause_snapshot`) since nothing else carries them forward. An `INSURED_FROM_DATE` change also shifts `Policy.expiry_date` by the same amount off `PolicyApplication.coverage_end_at` (not just `effective_date` off `coverage_start_at`) — the original period length is preserved, not just its start. The optional body — `{ coc_number?, sa_number? }` — lets the approver attach a COC/SA number at the moment of approval, written straight onto the created `Policy` row (see `Policy.coc_number`/`sa_number` below); both stay `null` when omitted, since not every policy gets one assigned at issuance. The `application` fetch also pulls everything needed to populate every `*_snapshot` column described under `Policy`/`PolicyVehicle`/`PolicyAddress`/`PolicyCoverage` in the Database section below (agent code, variant/class name, deductible/repair-limit rate, full vehicle/address/coverage detail, the charges breakdown). Deliberately single-stage — there's no `PENDING_MANAGER_APPROVAL`/`PENDING_UNDERWRITING_APPROVAL` multi-step chain yet, even though `ApplicationStatus` has room for one; any caller with `APPROVE_APPLICATION` can approve directly. No reject route exists yet either. If `send_policy_to_email_on_approval` was checked, sends the "your policy has been approved" email (`lib/applicationEmails.js`) after the transaction commits — the attached PDF is the issued Policy's own final, *signed* document: the just-created `Policy` row is re-fetched with its snapshot children (`routes/policies.js`'s `policyDetailSelect`) and mapped through that same file's `toPolicyPdfProps()` into `pdf/policyPdf.js`'s `buildPolicyPdf()` — the identical mapping `GET /policies/:id/pdf` uses for every later re-export, so the emailed copy and any future re-export can never disagree. A send failure here is caught and logged, never surfaced — the approval has already committed by that point. |
| `applyChangesToDetail(detail, changes)` | — | Not a route, and not defined in this file — imported from `routes/policyApplications.js` (see that router's own entry above for the full description) so `GET /:id`/`GET /:id/pdf` here and `GET /:id`/`GET /:id/pdf`/`GET /:id/changes`/`POST /:id/resend-email` there all fold recorded changes in identically. |

**`routes/policies.js`** — router-level `requireAuth, requirePermission("VIEW_POLICIES")`, mounted at `/policies`
| Method & path | Schema | Notes |
|---|---|---|
| `GET /` | `listPoliciesQuerySchema` | Client Policies page (`/my-policies`) — every issued `Policy` for the caller's own agent (`agent_id`, via `getCurrentAgentId()`), latest-issued first. Own-agent-only, same scoping as `GET /policy-applications`; no admin/cross-agent tier exists here (nothing asked for one the way Policy Approval has). 400 if the caller has no linked agent. |
| `GET /:id` | `policyIdParamSchema` | Flat JSON detail for one policy (the Client Policies page's row-detail dialog title/summary) — 404 unless owned by the caller's own agent. |
| `GET /:id/pdf` | `policyIdParamSchema` | The issued policy's final, *signed* PDF (`pdf/policyPdf.js`) — built purely from the `Policy` row's own `*_snapshot` columns (`policyDetailSelect`, mapped via `toPolicyPdfProps()`), never a live join back to `Vehicle`/`Agent`/`ProductVariant`/`ProductCoverage`, so it's guaranteed to still read exactly as approved regardless of what's since changed on those catalog rows. `toPolicyPdfProps`/`policyDetailSelect` are attached onto the exported `router` (same pattern as `policyApplications.js`'s own attached helpers) so `routes/policyApproval.js`'s approval-notification email can reuse the identical mapping for its own PDF attachment — one mapper, so the emailed copy and any later re-export can never disagree with each other. |
| `POST /:id/resend-email` | `policyIdParamSchema` | Re-sends the signed policy PDF to whatever email is on file for the insured customer/company — the Client Policies page's "Resend to client" action, mirroring `policyApplications.js`'s/`policyQuotations.js`'s own resend routes. Email is looked up live (`policyDetailSelect`'s `customer`/`company` relations), never off a snapshot — the same "never freeze an email address" reasoning `toApplicationDetail()`'s own `insured_email` already follows, since a resend should always reach whatever address is currently on file. 400 if neither party has one. |
| `GET /:id/renewal-prefill` | `policyIdParamSchema` | Everything the Policy Applications wizard needs to open pre-filled from this policy — the Client Policies page's "Renew This Policy" action (see `PolicyApplication.jsx`'s `renewalPrefill` prop below). Deliberately reads the *live* `Vehicle`/`Address` rows behind the policy's own `vehicle_id`/`address_id` FKs, not the frozen `*_snapshot` columns `policyDetailSelect` above uses — a renewal should carry forward whatever these records look like right now (corrections included), the same "reuse an on-file record" shape the wizard's own plate-lookup/address-`Autocomplete` flows already produce (`existing_vehicle_id`/`existing_address_id`/`existing_customer_id`/`existing_company_id`), not a stale copy of how they looked at the original policy's issuance. Also returns `min_coverage_start_at` (the policy's own `expiry_date` — see `POST /policy-applications`'s own enforcement of this below) and a `coverage_period_days` guess (the source policy's own effective-to-expiry length, in whole days). Coverage selections can only ever be reconstructed as "applies to all vehicles" (`vehicle_indices: null`) — `PolicyCoverage` rows don't retain which vehicle a coverage was originally scoped to (see `PolicyCoverage` below) — the agent re-narrows it on the form same as any other application. |

**`routes/users.js`** — router-level `requireAuth` only, each route adds its own `requirePermission`
| Method & path | Schema | Notes |
|---|---|---|
| `GET /` | — | `MANAGE_USERS`. Includes `specialPermissions` (direct grants beyond role). |
| `GET /roles` | — | `requireAuth` only. |
| `GET /permissions` | — | `requireAuth` only. Sub-permissions (dotted codes) grouped by parent. |
| `POST /roles` | `createRoleSchema` | `MANAGE_SETTINGS.CREATE_ROLE`. 409 on duplicate `role_name`. |
| `POST /` | `createUserSchema` | `MANAGE_USERS` + `MANAGE_USERS.ADD_USER`. Optionally creates linked `Customer`/`Agent`; emails the invite link via `lib/mailer.js` and reports `emailSent` in the response — a send failure doesn't fail user creation (the invite link is still logged to console and returned in the response either way, as a manual-share fallback). |
| `PATCH /:id` | `updateUserSchema` | Field-gated: details → `EDIT_USER_DETAILS`, `role_id` → `EDIT_ROLE`, `permission_ids` → `EDIT_SPECIAL_PERMISSIONS`. Email/password change or `reset_password` forces re-verification. |
| `PUT /roles/:id/permissions` | `updateRolePermissionsSchema` | `MANAGE_SETTINGS.EDIT_ROLE_PERMISSIONS`. Replace-all. |

**`routes/vehicles.js`** — router-level `requireAuth, requirePermission("CREATE_APPLICATION")`
| Method & path | Schema | Notes |
|---|---|---|
| `GET /lookup` | `lookupVehicleQuerySchema` | Case-insensitive plate match across **all** vehicles (not agent-scoped) — powers the plate-match/reassignment flow. 404 if not found. |
| `PATCH /:id` | `updateVehicleSchema` | Requires `agentCanEditVehicle`. Once `initial_assessment_date` is set, `estimated_value`/`initial_assessment_date` can never be directly edited again (silently ignored). `no_of_seats` is always freely editable (no freeze rule) — required, not part of the value/date lock. |

### Shared application/quotation intake logic

`POST /policy-applications` and `POST /policy-quotations` share one pattern (applications add payment fields on top):
- Constants: `DOC_STAMPS_RATE = 0.125`, `VAT_RATE = 0.12`, `LGT_RATE = 0.002`.
- Number generator: `APP-{YYYYMMDD}-{6 hex}` / `QUO-{YYYYMMDD}-{6 hex}` (`crypto.randomBytes(3)`).
- `insured_type` is **derived server-side** from whichever of `customer_id`/`company_id` was sent, never trusted from the client.
- `className` (from `product_variant.insurance_class.class_name`) drives conditional requirements: Motor needs ≥1 vehicle; Property needs `risk_address`; Motor or Property needs `insured_address`.
- Ownership check: acting agent must have a `CustomerAgent`/`CompanyAgent` link to the insured party (403 otherwise).
- Vehicle values for `VALUE_PERCENTAGE` pricing resolved via `currentVehicleValue()` (depreciation); for Property (no vehicles), the same pricing mode instead resolves off the risk address's own `Address.estimated_value` (no depreciation — a property's value doesn't automatically decay the way a vehicle's does), looked up fresh from the DB for a reused address or taken from what was just entered for a new one, mirroring the vehicle-value lookup split. Passed into `resolveCoverageRows` as `addressValue`.
- Coverage pricing/validation delegated to `resolveCoverageRows()` (`lib/coveragePricing.js`) — shared engine, throws `HttpError`.
- Single `prisma.$transaction`: creates the header row, resolves each vehicle (reuse / reassign-to-this-party / create-new), creates coverage line rows, resolves risk/insured addresses (reuse or create; `estimated_value` persisted on a newly created address).
- **Vehicle value/date freeze rule** (also enforced in `PATCH /vehicles/:id`): once `initial_assessment_date` is set, it and `estimated_value` are never directly overwritten again — value only changes via automatic depreciation. No equivalent freeze exists for `Address.estimated_value` — it can be revised directly at any time via `PATCH /addresses/:id`.
- **`Vehicle.no_of_seats`** is required on every vehicle create/reassign-update in both transactions (`vehicleInputSchema`) — not part of the freeze rule above, always writable. Feeds the policy schedule's "1 DRIVER AND N OCCUPANTS OR PASSENGERS" endorsement line (see "PDF generation" below), off the *first* vehicle on the schedule.

### PDF generation (`src/pdf/`) — standard going forward

**Every policy/quotation document (preview, export, email attachment) is generated server-side with PDFKit, in its own file under `backend/src/pdf/`.** This is a project-wide rule, not just how quotations happen to work: any future document type (an eventual issued-`Policy` PDF, etc.) gets its own `backend/src/pdf/<name>Pdf.js` following the same pattern — not a browser-print/`window.print()` flow, and not Puppeteer/headless-Chrome (this container has no Dockerfile, so Chromium's system libraries — `libnss3`, `libatk-1.0-0`, etc. — aren't installed and never get a chance to be; Puppeteer would crash at launch here).

- **`pdf/theme.js`** — the only genuinely shared plumbing: `FONT_BODY`/`FONT_BODY_BOLD` (PDFKit's standard-14 `Times-Roman`/`Times-Bold` — see the font-choice note below for why these, not an embedded font, are used for body text), `FONT_WATERMARK` (`Helvetica-Bold`, matching the HTML mockup's `Arial, sans-serif` bold — deliberately a different, sans-serif family, since a watermark/rubber-stamp is meant to read as a separate annotation layered on top of the typeset document, not as part of its body text), `BASE_FONT_SIZE` (9.5 — a compact, professional body size for a dense legal/insurance form; printed policy schedules typically run 9–10pt, not a web page's 12px), `registerFonts(doc)` (registers only the currency-only `"Peso"`/`"Peso-Bold"` DejaVu Serif fonts — `Times-Roman`/`Times-Bold` are PDFKit standard fonts, always available without registration), `formatMoney`/`fmtDate`/`fmtDateTime`, `drawCurrency`/`drawCurrencyInline` (see the font-choice note below), `drawLogoWatermark`, `drawDiagonalStamp`, `drawSignatureBlock(doc, x, y, width, { signed })`, `SIGNATURE_BLOCK_HEIGHT` (the block's total drawn height — callers use it to reserve room above the page's bottom margin before drawing the block, see the signature note below), `labelValue`, `computeDeductibleFigures(amount, deductibleRate, authorizedRepairLimitRate)` (pure numeric helper, not layout — `{ deductible, authorizedRepairLimit }`, either `null` when its rate isn't configured; `authorizedRepairLimit` is `deductible * authorizedRepairLimitRate`, a multiple of the deductible, not of `amount` directly).
- **Font choice — body text is Times-Roman/Times-Bold (standard-14 AFM fonts), currency alone is DejaVu Serif (`"Peso"`/`"Peso-Bold"`)**: this project briefly tried unifying the *entire* document onto one embedded DejaVu Serif family (to fix price text visually mismatching the surrounding body font), but that introduced a much worse, genuinely broken-looking defect and was reverted — kept here so it doesn't get re-attempted the same way. DejaVu Serif's embedded kerning data is applied by fontkit's glyph-layout engine **regardless of the `.text()` `features` option** (confirmed by testing `{features: ["-kern"]}` and even `{features: []}`, a full OpenType-feature opt-out — neither changed the output at all, proving the adjustment isn't coming from a togglable GSUB/GPOS feature), and for ordinary body-length text it produces wildly oversized position adjustments on certain letter pairs — e.g. "PARTY" rendering with a visible gap inside the word. Confirmed by inspecting the PDF's own decompressed content stream directly: DejaVu Bold's `TJ` adjustments ran 70–175 units for pairs like "P-A"/"R-T", versus Times-Bold's normal 20–75-unit kerning for the identical text (Times-Roman/-Bold use PDFKit's separate, well-tested AFM-kerning-table code path, not fontkit's TTF layout engine, and never exhibited this). The defect is specific to **pdf.js's canvas rendering** (`components/PdfViewer.jsx`, used for every in-app inline preview) — it did not show up when re-verified via the Read tool's own PDF rendering or a native PDF viewer's "Print / Save as PDF" export, which is exactly why it read as "the exportable PDF looks fine but the preview looks bad." Short numeric/currency strings (digits, peso sign, comma, period) never triggered it in any test — confirmed clean — so DejaVu Serif stays scoped to currency only, same as the project's original approach: `drawCurrency`/`drawCurrencyInline` draw a peso amount's *entire* `"₱1,234.56"` string in one call in `"Peso"`/`"Peso-Bold"` at `BASE_FONT_SIZE * PESO_FONT_SIZE_SCALE` (DejaVu Serif's glyphs render visibly larger than Times-Roman's at the same nominal size, so this correction keeps a price visually flush with the Times-set label beside it), then restore the caller's Times font/`BASE_FONT_SIZE`. **Known limitation of the DejaVu currency fonts specifically, deliberately not worked around**: their ligature substitution (fi/fl/ffi) isn't correctly reverse-mapped in the embedded font's ToUnicode CMap (a PDFKit 2.0.4/fontkit limitation; not reachable via `features` or ZWNJ insertion, both tried) — irrelevant for currency strings (no such letter pairs), noted here only because it was investigated during the same debugging pass.
- **`pdf/quotationPdf.js`** → `buildQuotationPdf(props)` — always shows the red diagonal "POLICY NOT IN EFFECT / QUOTATION ONLY" stamp; title "QUOTATION", "Quotation No :", "Date Prepared:".
- **`pdf/policyApplicationPdf.js`** → `buildPolicyApplicationPdf(props)` — title "POLICY SCHEDULE", "Policy No :", "Date Issued:"; shows the same red diagonal `drawDiagonalStamp` watermark mechanism as a quotation's (text "POLICY NOT IN EFFECT / AWAITING APPROVAL") whenever `props.isPreview` is true — which, since an application is never itself the issued `Policy` (see `Policy.application_id`), is every application PDF this builder produces today, live draft or already-saved re-export alike. Never draws a signed signature block (`drawSignatureBlock(doc, x, y, width)`, `signed` omitted/false) — an application, pending or already-approved-but-viewed-through-this-builder, is never the final signed document; see `pdf/policyPdf.js` below for that.
- **`pdf/policyPdf.js`** → `buildPolicyPdf(props)` — the issued `Policy`'s own document (`routes/policies.js`'s `GET /:id/pdf`, and the approval-notification email's attachment). Same "POLICY SCHEDULE" layout as `policyApplicationPdf.js`, with three deliberate differences: never carries the "AWAITING APPROVAL" watermark (a `Policy` only exists once approved, never as a draft, so there's no `isPreview` concept here at all); "Date Issued:" reads `props.issueDate` (the policy's actual `issue_date`) rather than the live render time, so a re-export keeps reading the way it did on the day it was issued; and its signature block is drawn `{ signed: true }` — the real branch-head signature image, not a blank line (see the signature-protection note below). `props.cocNumber`/`props.saNumber`, when present, each grow a second header line — "COC No :" right below "Policy No :", "SA No :" right below "Date Issued:" — since neither has anywhere else to print. `props` shape: `policyNumber, cocNumber?, saNumber?, classNameLabel, variantName, insuredName, insuredAddress, agentCode, issueDate, coverageStartAt, coverageEndAt, vehicles, coverages, deductibleRate, authorizedRepairLimitRate, totalPremium, docStamps, vat, lgt, misc, totalAmount, remarks` — built by `routes/policies.js`'s `toPolicyPdfProps()` purely off a `Policy` row's own `*_snapshot` columns, never a live join.
- **Deliberately duplicated, not shared**: these three files independently implement the *same* page layout (title block, two-column info/charges table, vehicle table, coverage sections, forms/endorsements, witness/signature block, clause page) rather than delegating to one shared "policy schedule" renderer — matches how `QuotationCreator.jsx`/`PolicyApplication.jsx` are already two separate, near-identical files rather than one parameterized component. Editing one document type's layout can't accidentally change another's.
- **Manager signature protection (`pdf/assets/manager_signature.png`)**: never served over HTTP — `src/index.js` registers no `express.static` anywhere under `src/` (or anywhere at all), so this file is reachable only by this Node process's own filesystem reads inside `theme.js`'s `drawSignatureBlock()`, never by a public URL. That's the entire protection mechanism, and it's already in place for every asset under `pdf/assets/` (the Bethel logo and DejaVu fonts included) — **do not add a static mount for `src/pdf/assets` (or any `src/` subdirectory)**, since that would make the signature image directly downloadable by anyone with the URL.
- **Signature block layout (`drawSignatureBlock`)** — the company-name line and the printed `BRANCH_HEAD_NAME` line are pulled apart by a named `SIG_NAME_Y` offset specifically to open up a floating gap for the signature image between them, rather than the image sitting cramped on top of the printed name (scaled down alongside `BASE_FONT_SIZE`'s own reduction — see the font-choice note above — so the gap stays proportional to the smaller body text around it, not oversized relative to it). `signed: true` (only ever passed by `pdf/policyPdf.js`) draws the image centered in that gap — sized off a fixed target height (capped to the gap's own clear height, so it can never grow tall enough to collide with either neighboring line regardless of the source image's own aspect ratio) rather than a fraction of the block's own width, and floats there rather than overlapping either line's text — the way a wet-ink signature actually sits above a signature line, not stamped on top of it. Every caller that positions the block near a page's bottom margin must reserve `SIGNATURE_BLOCK_HEIGHT` (the block's own full height, exported from `theme.js`) below its `y` anchor — the three `*Pdf.js` builders' own `sigY` computation additionally clamps between a `sigFloor` (pin the block near the bottom for short content, matching "the on-screen preview's own placement") and a `sigCeiling` a fixed distance above the bottom-margin small print (the "ID No./Agent Code/Time/Date" line) — **don't let `sigY` grow unbounded off `doc.y + 30` alone**; on a page with enough content to push `doc.y` that far down, an unclamped signature block collides with that footer line (this actually happened once, while enlarging the block to make room for the floating signature above — the fix was adding `sigCeiling`, not shrinking the block back down).
- **Canonical prop shape**: both builders take the same props — `applicationNumber, isPreview, classNameLabel, variantName, insuredName, insuredAddress, agentCode, coverageStartAt, coverageEndAt, vehicles, coverages, deductibleRate, authorizedRepairLimitRate, totalPremium, docStamps, vat, lgt, misc, totalAmount, remarks` (vehicle objects keep the DB's snake_case field names — `plate_number`, `mv_file_no`, `no_of_seats`, etc.; each `coverages[]` entry also carries `pricing_mode`, off `ProductCoverage.pricing_mode`). One shape, two consumers: every PDFKit builder, and the `documentPreviewPropsSchema` validating live draft data over the wire. `policyQuotations.js`'s/`policyApplications.js`'s `toPreviewProps(detail)` map a saved record's DB-flattened detail onto this same shape, including `deductible_rate`/`authorized_repair_limit_rate` off the record's own `product_variant`. (There used to be a third consumer — an HTML component mirroring this shape for an on-screen preview — see "Printing/PDF" below for why that's gone.)
- **Scheduled Vehicle table** — two columns × four rows, identical in all three builders: `Model:`/`MV File No.:`, `Body:`/`Serial No.:`, `Make:`/`Authentication No.:`, `Plate No.:`/`Color:`. These are this document's own labels for existing `Vehicle` fields, not new data — `Model:` reads `year_model` (this schedule's own long-standing convention: "Model" here means the model *year*, matching how a PH OR/CR's own "Model" field reads, not the separate `model` name field, which this schedule has never printed), `Body:` is `vehicle_type`, `Serial No.:` is `chassis_number`, and `Authentication No.:` is `engine_number` — a straight relabel/regroup of the same fields a now-removed three-column layout used to show as "Vehicle type:"/"Chassis No.:"/"Engine No.:".
- **Coverage sections** (the former single "COVERAGES" heading + table is now three sections, identical logic in all three builders): coverages are split by `pricing_mode` — **Section I/II** is a fixed, non-data-driven "THIRD PARTY LIABILITY (Subject to the Schedule of Indemnities) PREMIUMS" header (third-party liability is baked into the base Motor Car policy, not itemized as its own priced row here); **Section III "Insured's Estimate of Value of Scheduled Vehicle"** renders only when at least one coverage has `pricing_mode: "VALUE_PERCENTAGE"` — one block per such coverage row (its `amount` is already that row's targeted vehicle's current depreciated value, computed by `lib/coveragePricing.js`), each showing the estimate-of-value/premium, a static "Limit on Accessories — Standard Built-in Accessories" line, then a `Deductible: ₱x | Towing: | Authorized Repair Limit: ₱y` line via `computeDeductibleFigures()` (an em dash when a rate isn't configured on the coverage's `ProductVariant`; **Towing is always left blank** — there's no computed/stored value for it, filled in by hand on the printed copy); **"SECTION IVA, IVB, PA, AOG & OTHERS"** is every other coverage (`PERCENTAGE`/`FLAT_TIER`), same plain Coverage/Amount Covered/Premium table as before this split existed (a "None" row when there are no such coverages). The Deductible/Towing/Authorized Repair Limit line draws its two peso amounts via `theme.js`'s `drawCurrencyInline()` — same size-corrected DejaVu-for-currency-only handling as `drawCurrency()`, just for an amount flowing inline with label text on a continued-text run instead of sitting in its own right-aligned column (see the font-choice note above).
- **Forms & Endorsements / sea-faring notice** — Motor-specific boilerplate, rendered only when `vehicles.length > 0` (skipped entirely for Property): "1 DRIVER AND N OCCUPANTS OR PASSENGERS AT Php. 50,000.00 EACH" where N is `vehicles[0].no_of_seats - 1` (an em dash if seats is missing/invalid), followed by the fixed sea-faring-vessels/marine-cargo-policy advisory paragraph.
- **Witness paragraph + small print** — "IN WITNESS WHEREOF..." is fixed boilerplate naming Marikina (Bethel's branch city, hardcoded same as `theme.js`'s `BRANCH_HEAD_NAME`) and today's date (`fmtDate(new Date())` — the render date, not a stored "policy date" field); immediately followed by the existing Documentary-Stamps-paragraph/signature-block split (unchanged), then a small 8pt bottom-left line — `ID No.: ____________ / Agent Code: <agentCode> / Time: <hh:mm> / Date: <date>` — the physical form's own "ID No./Agent-Code/Time/Date" print; there's no "ID No." concept anywhere in this system, so that segment is always blank for hand entry (same reasoning as Towing above), while Agent Code/Time/Date are the document's real values.
- **Peso sign (₱) gotcha**: PDFKit's standard-14 fonts use WinAnsi encoding, which has no ₱ glyph — it silently renders as "±". See the font-choice note above for the fix (bundled DejaVu Serif, currency amounts only) and for why the obvious-looking alternative — embedding DejaVu Serif as the *body* font too, so there's only ever one font in play — was tried and reverted.
- **`pdf/assets/`** — `bethel-logo.png` (copied from `frontend/src/assets/`, so the backend has no dependency on the frontend build), `manager_signature.png`, and `fonts/DejaVuSerif(.ttf|-Bold.ttf)` (currency amounts only — see the font-choice note above; body text is PDFKit's standard-14 Times-Roman/Times-Bold, no font file to bundle for it).

### Middleware (`src/middleware/`)

- **`auth.js`** → `requireAuth`: reads `Authorization: Bearer <token>`, verifies JWT, sets `req.user = { userId, email }`.
- **`permissions.js`** → `requirePermission(code)` (Express middleware, 403 on missing), `ensurePermission(res, codes, code)` (non-middleware helper for field-gated single routes — caller must `return` on `false`), `getUserPermissionCodes(userId)` (unions role-granted + direct `UserPermission` grants; returns an **empty set** if `status` is `INACTIVE`/`SUSPENDED`, regardless of grants).
- **`validate.js`** → `validateBody`, `validateQuery`, `validateParams`: zod `safeParse` wrapper, 400s with `{ error: "<field>: <message>" }` (first issue only). `validateQuery`/`validateParams` use `Object.defineProperty` (not plain assignment) because Express 5 makes `req.query`/`req.params` read-only getters.

### Lib (`src/lib/`)

- **`agent.js`** → `getCurrentAgentId(userId)`: looks up `user.agent_id`.
- **`applicationEmails.js`** → `buildSubmissionEmailContent(detail)`/`buildApprovalEmailContent(detail)`: pure functions returning `{ subject, html, text }` for the two automatic `PolicyApplication` emails (see `send_policy_to_email`/`send_policy_to_email_on_approval` below) — kept as one shared lib, unlike the PDF builders in `src/pdf/` (which are deliberately duplicated per layout), because this is the exact same wording reused verbatim from three call sites (`POST /policy-applications`, `POST /policy-quotations/:id/submit`, `POST /policy-approval/:id/approve`), not a per-document layout that could ever need to diverge. Neither function sends anything itself — every call site builds its own PDF and calls `mailer.js`'s `sendMail()` directly, wrapped in a try/catch that logs and swallows a failure rather than failing the request that triggered it (the primary action — create, submit, or approve — has already succeeded by the time the email is attempted).
- **`coveragePricing.js`** → `round2`, `resolveCoverageRows({ coverages, className, vehicles, vehicleValues, agentId, startAt, endAt })`: the shared pricing engine for applications+quotations. Per coverage: confirms it's offered at the exact period (`endAt - startAt` must match an `allowable_periods.coverage_in_days`); resolves rate/tier by `pricing_mode` (agent override wins over product default, `VALUE_PERCENTAGE` uses depreciated vehicle value via a tax-bracket tier lookup, `FLAT_TIER` matches a fixed amount/price pair, `PERCENTAGE` validates against an effective max); **floor enforcement**: agent-entered `premium_amount` can never be below computed `payable_to_bethel` (throws `HttpError(400)` otherwise).
- **`httpError.js`** → `HttpError` (carries `status`+`body`), `sendIfHttpError(err, res)` (writes the response and returns `true` if `err instanceof HttpError`, else `false` so the route's `next(err)` still runs) — lets deep helpers throw typed HTTP errors without threading `res` through.
- **`mailer.js`** → `sendMail({ to, subject, html, text, attachments })`: lazy-cached `nodemailer` transport from `SMTP_HOST/PORT/USER/PASS/FROM` env vars; throws `HttpError(503, ...)` if unconfigured rather than crashing the server at boot. Sets an explicit `name` (EHLO hostname) from the sending domain — without it, nodemailer can't determine a real hostname inside Docker's network namespace and identifies itself as `EHLO [127.0.0.1]`, which mail servers treat as a spam signal and silently accept-then-never-deliver (no bounce). Used by quotation resend, by `auth.js`'s forgot-password and `users.js`'s invite/re-verification flows (the latter two always log the link too and never let a send failure change their response/fail the request, since a link stays reachable manually as a fallback — and forgot-password's anti-enumeration response has to stay identical either way), and by the two automatic `PolicyApplication` emails built via `applicationEmails.js` (`POST /policy-applications`, `POST /policy-quotations/:id/submit`, `POST /policy-approval/:id/approve` — see those routes' own entries and `PolicyApplication.send_policy_to_email`/`send_policy_to_email_on_approval` below).
- **`policyConflicts.js`** → `assertVehiclesFree(vehicleIds, coverageStartAt, coverageEndAt)`, `assertRiskAddressFree(addressId, coverageStartAt, coverageEndAt)`: the "no double-insuring the same asset" rule `POST /policy-quotations/:id/submit` enforces before converting — throws `HttpError(409)` on either vehicle (Motor) or `RISK`-role address (Property) if:  (1) a `PolicyApplication` for the same vehicle/address is still pending approval (`status` in `DRAFT/SUBMITTED/FOR_EDIT_*/PENDING_*_APPROVAL` — `REJECTED` doesn't count, it freed up whatever it referenced; `APPROVED` doesn't count either, since condition 2 governs it from that point on), or (2) an `ACTIVE` `Policy` already on file for the same vehicle/address has its own `[effective_date, expiry_date]` overlapping the new quotation's `[coverage_start_at, coverage_end_at]` — a half-open interval overlap test (`existing.effective_date < new.end AND existing.expiry_date > new.start`), not "any active policy ever": a policy that already expired before this one starts, or starts after this one ends, isn't a conflict.
- **`prisma.js`** → singleton `PrismaClient` via `PrismaPg` adapter; client generated to `backend/generated/prisma` (custom output path).
- **`vehicleValue.js`** → `currentVehicleValue(estimatedValue, initialAssessmentDate, asOf)`: `estimatedValue * (1 - 0.1)^wholeYearsElapsed` (`ANNUAL_DEPRECIATION_RATE = 0.1`, anniversary-exact year counting); `findApplicableValueTier(tiers, value)`: highest `min_value` ≤ value. Mirrored client-side (non-authoritative) in `frontend/src/utils/vehicleValue.js`.

### Utils (`src/utils/jwt.js`)

`signToken`/`verifyToken` wrapping `jsonwebtoken`; throws at module load if `JWT_SECRET` unset (fail-fast); `JWT_EXPIRES_IN` defaults `"8h"`.

### Zod schema files (`src/schemas/`)

- **`common.js`**: `requiredString(label)`, `requiredEmail(label)` — used everywhere.
- **`policyIntakeShared.js`** (shared by applications + quotations): `addressInputSchema`, `vehicleInputSchema` (deliberately omits `initial_assessment_date` — server-stamped only; `no_of_seats` is required — a positive whole number, see `Vehicle.no_of_seats` below), `coverageSelectionSchema` (`vehicle_indices` null/omitted = all vehicles, `.nonempty()` if present; `coverage_amount`/`premium_amount` are `.positive()`, not `.nonnegative()` — a 0 is what an empty form field coerces to, not a real selection), `refineWholeDayPeriod`/`wholeDayPeriodRefinement` (end must be a positive whole-24h-multiple after start), `refineExactlyOneParty`/`exactlyOnePartyRefinement` (exactly one of `customer_id`/`company_id`), `paymentFieldsSchema`/`refineBethelPaymentMethod`/`bethelPaymentMethodRefinement` (the `payment_method`/`payment_remittance`/`bethel_payment_method_id` shape + its conditional-required refinement, shared by `createApplicationSchema` and `submitQuotationSchema`), `documentPreviewPropsSchema` (the shared, permissive schema for both `POST /policy-quotations/preview-pdf` and `POST /policy-applications/preview-pdf` — see "PDF generation" above; mostly-optional fields since it's a read-only render, not a persisted write; `deductibleRate`/`authorizedRepairLimitRate` at the top level and `pricing_mode` per `coverages[]` entry drive the Section III vs. Section IVA/IVB/PA/AOG & Others split, `no_of_seats` per `vehicles[]` entry drives the Forms & Endorsements occupant count).
- **`policyApplications.js`**: `createApplicationSchema` = intake shape + `send_policy_to_email`/`send_policy_to_email_on_approval` (both optional booleans — see `PolicyApplication`'s own two fields below) + `policyIntakeShared.js`'s `paymentFieldsSchema` (`payment_method` enum `CASH/CHECK/CREDIT_CARD/BANK_TRANSFER/ONLINE_PAYMENT`, `payment_remittance` enum `DIRECT_TO_BETHEL/THROUGH_AGENT`, `bethel_payment_method_id` required via refinement when remittance is `DIRECT_TO_BETHEL`). `insured_type` explicitly not accepted from client.
- **`policyQuotations.js`**: `createQuotationSchema` = same intake shape minus all payment fields, plus an optional `agent_id` (shape-only here — a valid UUID string; whether it's actually honored, versus 403ed or ignored, is `POST /`'s own `resolveWriteAgent()` permission check, not something a static schema can express); `updateQuotationSchema` (coverage period + `send_policy_to_email` + coverages only, for `PATCH /:id`); `submitQuotationSchema` = `policyIntakeShared.js`'s `paymentFieldsSchema`, extended with optional `send_policy_to_email` and `send_policy_to_email_on_approval` (both asked fresh at submit time rather than carried over from the quotation's own `send_policy_to_email` — a quotation has no value at all to carry the latter over from, and the former means something different for a quotation than for the application it's about to become), + the Bethel-payment-method refinement, for `POST /:id/submit`; `listQuotationsQuerySchema` (`page`, `page_size` capped at 100); `quotationIdParamSchema` (`id` must be valid UUID) — used by `GET /:id`, `PATCH /:id`, `GET /:id/pdf`, `POST /:id/resend-email`, and `POST /:id/submit`.
- **`addresses.js`**: `updateAddressSchema`.
- **`agents.js`**: `getAgentRatesQuerySchema`, `netrateEntrySchema`/`updateNetratesSchema`, `agentValueTierSchema`/`updateAgentValueTiersSchema`, `agentFlatTierSchema`/`updateAgentFlatTiersSchema`.
- **`auth.js`**: `loginSchema`, `forgotPasswordSchema`, `setPasswordSchema` (password min 8 chars).
- **`catalog.js`**: `updateCoverageSchema` (both fields optional — PATCH only touches the field-group the caller has permission for); `productVariantIdParamSchema` (`id` must be a valid UUID) + `updateProductVariantRatesSchema` (`deductible_rate`/`authorized_repair_limit_rate`, both optional-and-nullable — either can be set or cleared independently) — `PATCH /product-variants/:id`.
- **`companies.js`**: `companyInputSchema` (used identically for create and update, no partial variant); `agentIdParamSchema` (`agentId` must be a valid UUID) — used by `GET /agent/:agentId` only.
- **`coveragePricing.js`**: exports `PRICING_MODES` constant; `getPricingQuerySchema`, `updatePricingModeSchema`, `valuePercentageTierSchema`/`updateValuePercentageTiersSchema`, `flatTierSchema`/`updateFlatTiersSchema`, `createAllowablePeriodSchema`.
- **`customers.js`**: `customerInputSchema` (used identically for create and update); `agentIdParamSchema` (`agentId` must be a valid UUID) — used by `GET /agent/:agentId` only, same shape as `companies.js`'s own (deliberately not shared — same small-per-file-duplication precedent as `applicationIdParamSchema`/`quotationIdParamSchema`).
- **`paymentMethods.js`**: `createPaymentMethodSchema`.
- **`policyApproval.js`**: `listAllApplicationsQuerySchema` (same shape as `listApplicationsQuerySchema`) — `:id` params reuse `applicationIdParamSchema` from `schemas/policyApplications.js` directly rather than a duplicate. `approveApplicationSchema` — `POST /:id/approve`'s optional body, `{ coc_number?, sa_number? }` (both trimmed strings, max 50 chars, blank normalized to `undefined` rather than persisted as `""`).
- **`policies.js`**: `listPoliciesQuerySchema` (same shape/cap as every other tracker list); `policyIdParamSchema` (`id` must be a valid UUID) — used by `GET /:id`/`GET /:id/pdf`.
- **`policyApplicationChanges.js`**: `APPLICATION_CHANGE_TYPES` (mirrors the Prisma `ApplicationChangeType` enum), `VEHICLE_CHANGE_TYPES`/`CLAUSE_CHANGE_TYPES` (the subsets needing `application_vehicle_id`/`application_coverage_id` respectively — also imported directly by `routes/policyApproval.js`, not just used for validation), `createApplicationChangeSchema` (`change_type` + conditionally-required `application_vehicle_id`/`application_coverage_id` via `superRefine` + optional `new_value` + optional `new_address` + optional `effective_date`/`remarks` — `change_from`/`change_to` are deliberately not accepted fields at all, since the route always computes them itself). `new_value` is required for every change type except `INSURED_ADDRESS_DETAILS` (enforced via `superRefine`, since a plain `.optional()` can't express "required unless"), which instead requires the structured `new_address` — reuses `updateAddressSchema` (`schemas/addresses.js`, the same shape `PATCH /addresses/:id` already accepts) minus `estimated_value` — since an address has too many independently-editable columns (line 1/2, barangay, city, province, postal code, country) for one string. Used by `POST /policy-approval/:id/changes`.
- **`users.js`**: `USER_STATUSES` constant; `requiresAgentCodeWhenMakingAgent`/`agentCodeRefinement` (shared refinement); `createRoleSchema`, `createUserSchema`, `updateUserSchema` (all fields optional — route does per-field-group permission checks), `updateRolePermissionsSchema`.
- **`vehicles.js`**: `updateVehicleSchema` (omits `initial_assessment_date`, same as `vehicleInputSchema`; `no_of_seats` required, same as `vehicleInputSchema`), `lookupVehicleQuerySchema`.

---

## Database — Prisma (`backend/prisma/schema/*.prisma`)

Multi-file schema (`backend/prisma.config.ts`: schema dir `prisma/schema`, migrations `prisma/migrations`, seed `node prisma/seed.js`). `schema.prisma` itself only declares the generator (`prisma-client-js`, output `../../generated/prisma`) and the `postgresql` datasource — no models there.

### Enums (`enums.prisma`)

`UserStatus`(AWAITING_EMAIL_VERIFICATION/ACTIVE/INACTIVE/SUSPENDED), `RecordStatus`(ACTIVE/INACTIVE), `CustomerStatus`(ACTIVE/INACTIVE/BLACKLISTED), `AddressType`(RESIDENTIAL/OFFICE/WAREHOUSE/BRANCH/RISK_LOCATION), `InsuredType`(INDIVIDUAL/CORPORATE), `ApplicationStatus`(DRAFT/SUBMITTED/FOR_EDIT_MANAGER/FOR_EDIT_UNDERWRITING/PENDING_MANAGER_APPROVAL/PENDING_UNDERWRITING_APPROVAL/APPROVED/REJECTED), `PolicyStatus`(ACTIVE/EXPIRED/CANCELLED/LAPSED), `ApprovalDecision`(APPROVED/REJECTED/RETURNED_FOR_EDIT), `CancellationDecision`(APPROVED/REJECTED), `PaymentMethod`(CASH/CHECK/CREDIT_CARD/BANK_TRANSFER/ONLINE_PAYMENT), `PaymentRemittance`(DIRECT_TO_BETHEL/THROUGH_AGENT), `AddressRole`(RISK/INSURED), `CoveragePricingMode`(PERCENTAGE/VALUE_PERCENTAGE/FLAT_TIER), `ApplicationChangeType`(INSURED_FROM_DATE/INSURED_NAME_DETAILS/INSURED_ADDRESS_DETAILS/VEHICLE_MODEL/VEHICLE_MV_FILE/VEHICLE_PLATE_NO/VEHICLE_TYPE/VEHICLE_MAKE/VEHICLE_COLOR/VEHICLE_ENGINE_NO/VEHICLE_CHASSIS_NO/ADD_CLAUSE/REMOVE_CLAUSE) — see `PolicyApplicationChange` below, `ApplicationPolicyType`(NEW_POLICY/RENEWAL) — see `PolicyApplication.policy_type` below.

### The three parallel transactional entities — Quotation / Application / Policy

The core domain lifecycle. Each stage has its own fully independent child tables (never shared), so earlier-stage history stays immutable:

| Stage | Header (`*.prisma` file) | Vehicle join | Address join | Coverage line | Approval? | Payment info? |
|---|---|---|---|---|---|---|
| Quote | `PolicyQuotation` (quotations.prisma) | `PolicyQuotationVehicle` | `PolicyQuotationAddress` | `QuotationCoverage` | No | No |
| Application | `PolicyApplication` (applications.prisma) | `PolicyApplicationVehicle` | `PolicyApplicationAddress` | `ApplicationCoverage` | Yes (`ApprovalHistory`) | Yes (`payment_method`, `payment_remittance`, `bethel_payment_method_id`) |
| Issued policy | `Policy` (policies.prisma) | `PolicyVehicle` (+ snapshot fields) | `PolicyAddress` (+ snapshot field) | `PolicyCoverage` (+ snapshot fields) | Cancellation only (`CancellationHistory`) | N/A (settled at application stage) |

- **PolicyQuotation**: priced identically to an application (same coverage/pricing engine). Can be edited in place (`PATCH /policy-quotations/:id` — coverage period, `send_policy_to_email`, and coverages only; re-prices through the same `resolveCoverageRows` engine) and can be converted into a `PolicyApplication` (`POST /policy-quotations/:id/submit`) — see `PolicyApplication.source_quotation_id` below. Once converted, a quotation is frozen: both the edit and submit routes 409 on a quotation whose `converted_application` relation is already set. Fields: `id, insured_type, quotation_number (unique), customer_id?, company_id?, company_name_snapshot?, agent_id, agent_name_snapshot?, product_variant_id, coverage_start_at, coverage_end_at, quotation_date, total_premium, doc_stamps, vat, lgt, misc, send_policy_to_email, remarks?, created_at, updated_at`. Grand total (`total_premium+doc_stamps+vat+lgt+misc`) is never stored, always computed on read (see `toQuotationDetail()` in the route).
- **PolicyApplication**: adds `application_number (unique)`, `status: ApplicationStatus`, `application_date`, `submission_date`, `payment_method`, `payment_remittance`, `bethel_payment_method_id?` (FK → `AuthorizedPaymentMethod`, only set when remittance is `DIRECT_TO_BETHEL`), `document_folder_name?`, `source_quotation_id?` (FK → `PolicyQuotation`, `@unique` — set only when this application was created by converting a quotation rather than filed fresh; a given quotation can convert at most once), `send_policy_to_email` (emailed right after this application is filed/submitted — the "now under approval, please settle payment" notice, see `lib/applicationEmails.js`'s `buildSubmissionEmailContent`), `send_policy_to_email_on_approval` (emailed only once `POST /policy-approval/:id/approve` actually approves this application — the "your policy has been approved" notice with the issued `Policy`'s own PDF attached, see `buildApprovalEmailContent`; independent of the flag above — an agent can ask for either, both, or neither). `status` walks `DRAFT → SUBMITTED → FOR_EDIT_*/PENDING_*_APPROVAL → APPROVED/REJECTED`. `policy_type: ApplicationPolicyType` (`NEW_POLICY`/`RENEWAL`, `@default(NEW_POLICY)` at the DB level only so the migration adding it didn't fail on rows already on file) and `renewed_policy_id?` (FK → `Policy`, only set when `policy_type` is `RENEWAL`) are both always server-derived at `POST /policy-applications` from whether the client sent `renewed_policy_id` — see that route's own entry above and `routes/policies.js`'s `GET /:id/renewal-prefill`, which is what actually populates `renewed_policy_id` in the create payload from the Client Policies page's "Renew This Policy" action. The Policy Applications tracker's own "Type" column (and Policy Approval's) reads this field directly.
- **Policy**: `application_id` is `@unique` — strict 1:1 child of exactly one application (created only once approved, via `POST /policy-approval/:id/approve`). `policy_status: PolicyStatus`, `issue_date/effective_date/expiry_date`, `added_to_inlease`/`inlease_added_at` are its own live-owned fields. `coc_number`/`sa_number` (both optional `VarChar(50)`) are entered by the approver at the moment of approval (`POST /policy-approval/:id/approve`'s optional body) rather than collected anywhere earlier — a quotation/application never has either — and print on the issued policy PDF (`pdf/policyPdf.js`) as a second line right below "Policy No :"/"Date Issued:" respectively, whenever present. **Every other field a re-exported policy document would need is frozen at issuance as a `*_snapshot` column (or, for charges, plain owned columns) — nothing about the printed document is allowed to depend on a live FK that could still be edited afterward**, since (unlike a `PolicyQuotation`/`PolicyApplication`, which are still mid-flow) an issued `Policy` is the final, immutable stage:
  - `customer_name_snapshot`/`company_name_snapshot`/`agent_name_snapshot`/**`agent_code_snapshot`** (name *and* code, since only the name used to be frozen).
  - **`class_name_snapshot`/`variant_name_snapshot`** off `ProductVariant`/`InsuranceClass` — `product_variant_id` stays a live FK for reporting/joins, but a later rename must never change what an already-issued policy's PDF prints.
  - **`deductible_rate_snapshot`/`authorized_repair_limit_rate_snapshot`** off `ProductVariant.deductible_rate`/`authorized_repair_limit_rate` — feeds the policy schedule's Section III Deductible/Authorized Repair Limit line (`pdf/theme.js`'s `computeDeductibleFigures()`); a later edit via Settings → Vehicle Rates must never change the figures on an already-issued policy's re-export.
  - **`total_premium`/`doc_stamps`/`vat`/`lgt`/`misc`/`remarks`** — copied verbatim from `PolicyApplication` at issuance, never recomputed from the `DOC_STAMPS_RATE`/`VAT_RATE`/`LGT_RATE` constants again afterward (those constants could themselves change in a future code deploy; a `Policy`'s own charges must not move if they do). Grand total is still never stored, computed on read the same way as the other two stages.
  - **`PolicyVehicle`**: `vehicle_id` stays a live FK (ownership/depreciation still need it), but `plate_number_snapshot`/`mv_file_no_snapshot`/`engine_number_snapshot`/`chassis_number_snapshot`/`make_snapshot`/`model_snapshot`/`year_model_snapshot`/`vehicle_type_snapshot`/`color_snapshot`/`no_of_seats_snapshot` are frozen off `Vehicle` at issuance — necessary because a `PolicyApplicationChange`'s `VEHICLE_*` corrections write straight onto the *shared* `Vehicle` row (see `PolicyApplicationChange` below), so without a snapshot a correction logged against a later, unrelated application for the same repeat customer's vehicle would silently rewrite what an already-issued policy's PDF shows.
  - **`PolicyAddress`**: `address_id` stays a live FK, but `formatted_address_snapshot` (the same comma-joined shape `formatAddress()` in `routes/policyApplications.js` already produces) is frozen at issuance — `Address` rows have no freeze rule at all and are always editable via `PATCH /addresses/:id`.
  - **`PolicyCoverage`** additionally freezes `coverage_code_snapshot`/`coverage_name_snapshot`/`clause_snapshot`/**`pricing_mode_snapshot`** off `ProductCoverage` at issuance (catalog can change later, issued wording — and, now, which policy-schedule section a coverage prints under — must not) — `clause_snapshot` takes any `ADD_CLAUSE`/`REMOVE_CLAUSE` change recorded for that coverage line over the catalog's own `clause` text, and `effective_date` takes an `INSURED_FROM_DATE` change over `PolicyApplication.coverage_start_at`, if either was recorded (see `PolicyApplicationChange` below) — when it is, `expiry_date` shifts by the same amount off `PolicyApplication.coverage_end_at` rather than staying put, so the issued policy's period keeps its originally-quoted length instead of silently shrinking/stretching.
  - All of the above are populated in `routes/policyApproval.js`'s `POST /:id/approve` from data fetched fresh at approval time (so any `VEHICLE_*`/`INSURED_ADDRESS_DETAILS` correction already applied to the underlying row by then is naturally captured too — "as of approval" includes every correction recorded before approval happened) and built once into local variables (`vehiclesForPdf`, `coveragesForPdf`, `insuredAddressSnapshot`) shared by both the snapshot-row inserts and the approval-notification email's own PDF attachment, so the two can never disagree with each other. There's no client input to any of this — every new field is server-computed from already-validated upstream data, so no zod schema changes were needed for the approve route itself (it still takes no body).
  - No `routes/policies.js`/`GET .../pdf` exists yet to actually re-export an issued policy's document on demand (`/my-policies` is still a `PlaceholderPage` stub) — this is what a future such route would read from instead of live-joining `Vehicle`/`Address`/`Agent`/`ProductVariant`/`ProductCoverage`.
- Coverage line rows (`ApplicationCoverage`/`QuotationCoverage`) share the shape `coverage_amount, premium_amount, payable_to_bethel, applied_rate, {application|quotation}_vehicle_id?` (null = applies to all vehicles on that record) — `applied_rate` freezes the agent's net rate at submission time so historical branch revenue stays accurate even if rates change later; `premium_amount - payable_to_bethel` = the agent's profit on that row.
- **PolicyApplicationChange** (applications.prisma): an approver's audit trail of corrections against one `PolicyApplication`, recorded via `POST /policy-approval/:id/changes` without the `PolicyApplication` row itself ever being touched — see `routes/policyApproval.js` above. Fields: `policy_application_id (FK), application_vehicle_id? (FK → PolicyApplicationVehicle — which of the application's vehicles, for a Motor application with more than one), application_coverage_id? (FK → ApplicationCoverage — which coverage line, for AddClause/RemoveClause), change_type: ApplicationChangeType, change_from?, change_to?, effective_date?, remarks?, created_by_user_id (FK → User), created_at`. `change_from`/`change_to` are always computed server-side, never trusted from the client. For `VEHICLE_*` (all 8 individual `Vehicle` columns — model/mv_file_no/plate_number/vehicle_type/make/color/engine_number/chassis_number) and `INSURED_ADDRESS_DETAILS` (every column of the application's `INSURED`-role `Address` — `address_line_1`/`address_line_2`/`barangay`/`city`/`province`/`postal_code`/`country`, submitted as `new_address` rather than `new_value` — see `createApplicationChangeSchema` above), the new value is *also* written straight onto the underlying `Vehicle`/`Address` row (`change_to` itself stays the human-readable combined string, via `formatAddress()`), so the correction is already there the next time that same vehicle/address is selected on a future application/quotation — the catalog record changes permanently, not just this one application's picture of it. `ADD_CLAUSE`/`REMOVE_CLAUSE` instead amend a copy of the coverage's clause text scoped to `change_to` on this one change row — the shared `ProductCoverage.clause` catalog entry is never touched (that would silently reword every other application/quotation using that coverage too). `INSURED_FROM_DATE`/`INSURED_NAME_DETAILS` have no backing catalog record at all — logged only. Every change type's correction is folded back in wherever this application's own detail/PDF is re-rendered (`applyChangesToDetail()` in `routes/policyApplications.js`, see above) and, for `INSURED_FROM_DATE`/`INSURED_NAME_DETAILS`/clause changes specifically, into the issued `Policy` on approval (see above) — `VEHICLE_*`/`INSURED_ADDRESS_DETAILS` changes need no such folding-in anywhere, since every read of `Vehicle`/`Address` already sees the corrected row.

### Party model (`parties.prisma`, `locations.prisma`)

- `Customer`/`Company` are both "insureds" (`InsuredType` on quotes/applications says which). `Customer.company_id` optionally links a customer to a company. `Agent` is required (non-null `agent_id`) on every quote/application/policy.
- `PartyAddress`/`PartyVehicle` (locations.prisma) are unified join tables — `customer_id`/`company_id` both optional but mutually exclusive (one party, never both), explicitly documented as a merge of what used to be separate `Customer*`/`Company*` join tables so ownership queries run as a single OR.
- `CustomerAgent`/`CompanyAgent` are the many-to-many assignment tables gating which parties an agent may quote/apply/edit for (checked on every relevant write route).
- `Address`/`Vehicle` are party-agnostic catalog records; `Vehicle.initial_assessment_date` is stamped automatically the first time `estimated_value` is ever set, then frozen — see "freeze rule" above. `Vehicle.no_of_seats` (required `Int`, `@default(5)` at the DB level only so the migration adding it didn't fail on rows already on file — every application-level create/edit always supplies a real value) feeds the policy schedule's driver/occupants endorsement line, not the freeze rule.

### Pricing catalog (`catalog.prisma`, agent overrides in `parties.prisma`)

Spine: `InsuranceClass → ProductVariant → ProductCoverage → CoverageAllowablePeriod`. `ProductVariant.deductible_rate`/`authorized_repair_limit_rate` (both nullable `Decimal`) are set via Settings → Vehicle Rates (`PATCH /product-variants/:id`) — the policy schedule's Section III Deductible/Authorized Repair Limit line (see "PDF generation" above) is computed off whichever `VALUE_PERCENTAGE` coverage's variant these belong to; either or both left `null` just omits that figure rather than computing off an assumed-0 rate. `ProductCoverage.pricing_mode: CoveragePricingMode` selects one of three parallel mode families, each keyed by `coverage_allowable_period_id` (never by coverage directly — a coverage can price differently per allowable period) with a **coverage-level default** and an optional **agent-level override** that fully replaces it:

| Mode | Coverage default table | Agent override table |
|---|---|---|
| `PERCENTAGE` | `CoveragePercentageBasedPricing` (1:1, `standard_rate`) | `AgentNetrate` (`netrate`, optional `maximum_coverage`) |
| `VALUE_PERCENTAGE` | `CoverageValuePercentageTier` (tax-bracket tiers: `min_value`+`rate_percentage`) | `AgentValuePercentageTier` (same shape) |
| `FLAT_TIER` | `CoverageTierBasedPricing` (`coverage_amount`+`coverage_price` pairs) | `AgentFlatTierPricing` (same shape) |

### Access/audit (`access.prisma`, `history.prisma`)

- `User` optionally links to one `Agent` and/or one `Customer` (portal-style logins). RBAC: `Role`⟷`Permission` via `RolePermission`, plus direct `User`⟷`Permission` grants via `UserPermission` (independent of role), `User`⟷`Role` via `UserRole`.
- `Permission.permission_code` self-encodes hierarchy via dot-notation (`PARENT.CHILD`) rather than a separate grouping column — a code with no dot is a top-level page-access permission, a dotted one is scoped under that page.
- `ApprovalHistory` (per `PolicyApplication`) / `CancellationHistory` (per `Policy`) are append-only audit logs of every approver decision.

### Snapshot-field pattern

Many models carry `*_name_snapshot`/`*_snapshot` fields alongside the live FK (e.g. `PolicyApplication.company_name_snapshot`, `Policy.customer_name_snapshot`/`agent_name_snapshot`, `PolicyCoverage.coverage_code_snapshot`/`clause_snapshot`) — frozen display text as of that record's creation, so historical documents don't silently change if the referenced Company/Agent/Coverage is later edited.

### `payments.prisma`

`AuthorizedPaymentMethod` (`id, name (unique)`) — payment methods Bethel itself accepts when payment is remitted `DIRECT_TO_BETHEL` rather than collected by the agent first. Managed via Settings → Authorized Payment Methods.

---

## Frontend (`frontend/src/`)

### Routing table (`App.jsx`)

All routes except `/login`, `/forgot-password`, `/set-password` are nested under `<ProtectedRoute><AppLayout /></ProtectedRoute>` (redirects to `/login` if no token). Unknown paths redirect to `/dashboard`.

| Path | Component | Permission (`RequirePermission`) |
|---|---|---|
| `/login`, `/forgot-password`, `/set-password` | `Login`, `ForgotPassword`, `SetPassword` | none (public) |
| `/dashboard` | `Dashboard` | none (any logged-in user) |
| `/policy-application` | `PolicyApplications` | `CREATE_APPLICATION` |
| `/quotation-tracker` | `Quotations` | `QUOTATION_TRACKER` |
| `/my-policies` | `ClientPolicies` | `VIEW_POLICIES` |
| `/inlease-backlogs` | `PlaceholderPage` (stub) | `MANAGE_INLEASE` |
| `/policy-approval` | `PolicyApproval` | `APPROVE_APPLICATION` |
| `/manage-users` | `ManageUsers` | `MANAGE_USERS` |
| `/my-agents` | `MyAgents` | `MANAGE_AGENTS` |
| `/settings` | `Settings` | `MANAGE_SETTINGS` |
| `/settings/role-permissions` | `RoleDefaultPermissions` | `MANAGE_SETTINGS.EDIT_ROLE_PERMISSIONS` |
| `/settings/create-role` | `CreateRole` | `MANAGE_SETTINGS.CREATE_ROLE` |
| `/settings/edit-clauses` | `EditClauses` | `MANAGE_SETTINGS.EDIT_CLAUSES` |
| `/settings/payment-methods` | `AuthorizedPaymentMethods` | `MANAGE_SETTINGS.MANAGE_PAYMENT_METHODS` |
| `/settings/coverage-pricing` | `ManageCoveragePricing` | `MANAGE_SETTINGS.MANAGE_COVERAGE_PRICING` |
| `/settings/vehicle-rates` | `ManageVehicleRates` | `MANAGE_SETTINGS.MANAGE_COVERAGE_PRICING` |

Note: `In-Lease Backlogs` is still a stub page (`PlaceholderPage`) — not implemented. `My Policies` (nav label "Client Policies") is a real page — see `ClientPolicies` under Pages below. `Policy Approval` is a real page with a working (single-stage — see `POST /policy-approval/:id/approve` above) approve flow and a change-recording form; there's no reject flow yet. "New Quotation" used to be its own route (`/quotation-tracker/create`) — it's now a `Dialog` embedded in `Quotations.jsx` instead (see the Pages section below), so that route no longer exists.

### Permission-gating pattern

- `ProtectedRoute.jsx` — outer gate: no `token` → redirect `/login`.
- `RequirePermission.jsx` — inner gate: `permissions === null` (still loading) → render nothing; permission missing → redirect `/dashboard`; else render children.
- `AuthContext.jsx` — on mount/token change calls `getMe(token)`, stores `permissions` (array of codes) + `agent`; `permissions: []` on failure.
- `nav/navItems.js` — each item has `permission: null | "CODE"`; `AppLayout` filters the sidebar the same way `RequirePermission` gates routes.
- Fine-grained in-page gating beyond page access uses the same `permissions` array directly, e.g. `MyAgents.jsx` conditionally shows premium/rate columns; `PolicyApplication.jsx` disables Submit without `CREATE_APPLICATION.AGENT_ISSUANCE` (form stays fillable); `Quotations.jsx` hides "New Quotation" without `QUOTATION_TRACKER.CREATE_QUOTATION`/`.ADMIN_CREATE_QUOTATION`, shows the table only with `QUOTATION_TRACKER.VIEW_QUOTATION`/`.ADMIN_VIEW_QUOTATION`, and adds an Agent column with `.ADMIN_VIEW_QUOTATION` specifically; `QuotationCreator.jsx` shows a cross-agent "Filing Agent" picker only with `QUOTATION_TRACKER.ADMIN_CREATE_QUOTATION`.

### API client contract (`src/api/client.js`)

`request(path, { method, body, token })` — sets `Content-Type`, `Authorization: Bearer <token>` if given, JSON-encodes body, throws `Error(data?.error || "Request failed with status <n>")` on non-2xx. `requestBlob(path, { method, body, token })` is the same contract for the PDF endpoints, which return `application/pdf` instead of JSON — resolves to a `Blob` instead of parsed data.

| Function | Endpoint |
|---|---|
| `login`, `getMe`, `setPassword`, `forgotPassword` | `POST /auth/login`, `GET /me`, `POST /auth/set-password`, `POST /auth/forgot-password` |
| `listUsers`, `listRoles`, `listPermissions`, `createUser`, `updateUser`, `createRole`, `updateRolePermissions` | `GET /users`, `GET /users/roles`, `GET /users/permissions`, `POST /users`, `PATCH /users/:id`, `POST /users/roles`, `PUT /users/roles/:roleId/permissions` |
| `getProductCatalog`, `listCoverages`, `updateCoverage` | `GET /product-catalog`, `GET /coverages`, `PATCH /coverages/:id` |
| `listProductVariants`, `updateProductVariant` | `GET /product-variants`, `PATCH /product-variants/:id` — Settings → Vehicle Rates |
| `listMyCustomers`, `createCustomer`, `updateCustomer` | `GET /customers`, `POST /customers`, `PATCH /customers/:id` |
| `listMyCompanies`, `createCompany`, `updateCompany` | `GET /companies`, `POST /companies`, `PATCH /companies/:id` |
| `updateVehicle`, `lookupVehicleByPlate` | `PATCH /vehicles/:id`, `GET /vehicles/lookup?plate_number=` (404 → resolves `null`, not an error) |
| `updateAddress` | `PATCH /addresses/:id` |
| `createPolicyApplication`, `previewApplicationPdf`, `listApplications`, `getApplication`, `listMyApplicationChanges`, `downloadApplicationPdf`, `resendApplicationEmail` | `POST /policy-applications`, `POST /policy-applications/preview-pdf`, `GET /policy-applications?page=&page_size=`, `GET /policy-applications/:id`, `GET /policy-applications/:id/changes`, `GET /policy-applications/:id/pdf`, `POST /policy-applications/:id/resend-email` |
| `createPolicyQuotation`, `listQuotations`, `getQuotation`, `updateQuotation`, `submitQuotation`, `resendQuotationEmail`, `previewQuotationPdf`, `downloadQuotationPdf`, `listAgentsForQuotation` | `POST /policy-quotations`, `GET /policy-quotations?page=&page_size=`, `GET /policy-quotations/:id`, `PATCH /policy-quotations/:id`, `POST /policy-quotations/:id/submit`, `POST /policy-quotations/:id/resend-email`, `POST /policy-quotations/preview-pdf`, `GET /policy-quotations/:id/pdf`, `GET /policy-quotations/agents` |
| `listCustomersByAgent`, `listCompaniesByAgent` | `GET /customers/agent/:agentId`, `GET /companies/agent/:agentId` — `QUOTATION_TRACKER.ADMIN_CREATE_QUOTATION`'s cross-agent party pickers, alongside `listMyCustomers`/`listMyCompanies` (two rows above) |
| `listApplicationsForApproval`, `downloadApplicationForApprovalPdf`, `getApplicationForApproval`, `listApplicationChanges`, `createApplicationChange`, `approveApplication` | `GET /policy-approval?page=&page_size=`, `GET /policy-approval/:id/pdf`, `GET /policy-approval/:id`, `GET /policy-approval/:id/changes`, `POST /policy-approval/:id/changes`, `POST /policy-approval/:id/approve` — `approveApplication`'s payload is optional `{ coc_number?, sa_number? }` |
| `listAgents`, `getAgentNetrates`, `updateAgentNetrates`, `updateAgentValueTiers`, `updateAgentFlatTiers` | `GET /agents`, `GET /agents/:id/netrates?coverage_in_days=`, `PUT /agents/:id/netrates`, `PUT /agents/:id/value-percentage-tiers/:coverageId`, `PUT /agents/:id/flat-tiers/:coverageId` |
| `listMyPolicies`, `getPolicy`, `downloadPolicyPdf`, `resendPolicyEmail`, `getPolicyRenewalPrefill` | `GET /policies?page=&page_size=`, `GET /policies/:id`, `GET /policies/:id/pdf`, `POST /policies/:id/resend-email`, `GET /policies/:id/renewal-prefill` — Client Policies page |
| `listPaymentMethods`, `createPaymentMethod`, `deletePaymentMethod` | `GET /payment-methods`, `POST /payment-methods`, `DELETE /payment-methods/:id` |
| `getCoveragePricing`, `updateCoveragePricingMode`, `updateValuePercentageTiers`, `updateFlatTiers`, `createAllowablePeriod` | `GET /coverages/:id/pricing?coverage_in_days=`, `PATCH /coverages/:id/pricing`, `PUT /coverages/:id/value-percentage-tiers`, `PUT /coverages/:id/flat-tiers`, `POST /coverages/:id/allowable-periods` |

**When you add/change a backend route, add/update the matching `api/client.js` function in the same change** — it's the only place the frontend knows an endpoint exists.

### Pages (`src/pages/*.jsx`)

- **Login / ForgotPassword / SetPassword**: public auth flows; no client-side schema validation beyond HTML `required` + manual password-match check in `SetPassword`.
- **Dashboard**: fetches `getMe`, shows account info + status; warns if `INACTIVE`/`SUSPENDED`.
- **ManageUsers**: table of all users; `AddUserDialog`/`EditUserDialog` share a `RoleAndPermissionsFields` subcomponent; shows a manually-shareable invite link after create/reverification since email isn't actually sent.
- **Settings**: pure navigation hub (`List` of links to sub-pages), no data fetching.
- **RoleDefaultPermissions / CreateRole / EditClauses**: dirty-tracking edit pages (current vs. original state, `isDirty` via `useMemo`, Save/Cancel only shown when dirty).
- **AuthorizedPaymentMethods**: simple add/delete list, client-sorted alphabetically.
- **ManageCoveragePricing**: most complex settings page — `CoverageSelector` (class→variant→coverage) + period `Autocomplete` (with inline "add new period" flow) + `RadioGroup` of the 3 pricing modes, each with its own editable tier table; dirty-diffs 5 pieces of state before enabling Save.
- **ManageVehicleRates** (`/settings/vehicle-rates`, gated `MANAGE_SETTINGS.MANAGE_COVERAGE_PRICING`): `EditClauses.jsx`-style dirty-tracking page for one `ProductVariant` at a time (a flat class→variant picker, not `CoverageSelector` — there's no coverage level here) — two percent-shaped fields, `deductible_rate`/`authorized_repair_limit_rate` (stored as a 0–1 fraction, shown/entered as a whole percent, same `rateToPercentDisplay`/`percentDisplayToRate` convention as agent netrates), feeding the policy schedule's Section III Deductible/Authorized Repair Limit line (see backend's "PDF generation" section).
- **MyAgents**: agent table with permission-gated premium/rate columns; `RatesDialog` lets an admin set per-agent, per-coverage pricing overrides (net rate or tier table depending on the coverage's mode).
- **Quotations** (Quotation Tracker, `/quotation-tracker`, gated on `QUOTATION_TRACKER`): server-paginated table (row data already carries `quotation_number`, so no JSON detail fetch is needed for the dialog title); **row click opens `QuotationDetailDialog`**, which fetches the actual PDF (`downloadQuotationPdf`) and renders it inline via `PdfViewer`. "Re-export PDF" reuses that same already-fetched blob URL (`window.open(pdfUrl, "_blank")` — no second request). "Resend to client" via `resendQuotationEmail`. "New Quotation" opens `QuotationCreator` embedded in a `Dialog` (not a separate route — see below); hidden without `QUOTATION_TRACKER.CREATE_QUOTATION` or `.ADMIN_CREATE_QUOTATION` (`canCreate`). The table itself renders only with `QUOTATION_TRACKER.VIEW_QUOTATION` or `.ADMIN_VIEW_QUOTATION` (`canView`) — an info `Alert` in its place otherwise, since `QUOTATION_TRACKER` alone (page access) doesn't imply either. With `ADMIN_VIEW_QUOTATION` specifically (`isAdminView`), an extra **Agent column** appears (`agent_name`/`agent_code`, mirroring `PolicyApproval.jsx`'s own) since the list then spans every agent, not just the caller's. A **Status column** (`row.status`, see `GET /policy-quotations` above) renders "Submitted" or, once converted, a "For Issuance" chip plus a `RouterLink` to `/policy-application?open=<converted_application_id>` showing the resulting application's number — `onClick` on the link calls `stopPropagation` so it doesn't also trigger the row's own detail dialog. An **Actions column** (edit/submit icon buttons, `stopPropagation`ed so they don't also trigger the row-click detail dialog) opens `EditQuotationDialog`/`SubmitQuotationDialog`; both disabled once `row.converted` is true (the quotation has already become a policy application) or without `canCreate` — Submit is additionally disabled without `CREATE_APPLICATION.AGENT_ISSUANCE` (`canIssue`).
- **EditQuotationDialog**: edits a saved quotation's coverage period, `send_policy_to_email`, and coverages (only what `PATCH /policy-quotations/:id` accepts) — fetches `getQuotation` + `getProductCatalog`, regroups the quotation's already-per-vehicle-expanded coverage rows back into one editable selection per coverage (via each row's `vehicle_index`, which `toQuotationDetail` on the backend derives from `policy_quotation_vehicle_id`), and runs its own copy of `resolveCoverageSelection` (mirrors QuotationCreator.jsx's, same non-authoritative-preview reasoning) since vehicles here are the quotation's fixed list, not user-entered ones. A datetime-local input round-tripping an *existing* UTC timestamp is the one thing genuinely new here (versus QuotationCreator.jsx, which only ever goes form → submit, never display-existing → resubmit): `toLocalDateTimeInput` deliberately reads UTC getters, not local ones, because the value gets sent back to the server as a naive string that `z.coerce.date()` parses in the *server's* timezone (UTC, no `TZ` set) — using local (browser) getters would silently shift the date by the browser/server offset the instant an unrelated field is edited and this one is simply resubmitted unchanged. Its `buildPreviewProps()` also passes through each coverage's `pricing_mode` and the quotation's own `deductible_rate`/`authorized_repair_limit_rate` (both already on `detail`, off the quotation's `product_variant`) — same PDF-section-split inputs QuotationCreator.jsx's own preview carries, see backend's "PDF generation" section. Like QuotationCreator.jsx, "Preview changes" is required before "Confirm and save" — fetches `previewQuotationPdf` into a nested preview `Dialog`; unlike QuotationCreator.jsx, "Preview changes" also stays disabled until the form actually diverges from an `initialSnapshot` captured right after load (`coverageStartAt`/`coveragePeriodDays`/`coverageSelections`/`sendPolicyToEmail`, deep-compared via a `useMemo` `isDirty`) — there's nothing to preview or save about an unedited quotation. The form is split into distinct sections matching QuotationCreator.jsx's own: "Coverage Period" (dates + period checkboxes), then "Coverages", then a "Charges" breakdown (Premium/Doc. Stamps/V.A.T./L.G.T./Miscellaneous/Total Php.) mirroring QuotationCreator.jsx's own Charges `Paper` (`misc` itself stays display-only here since `updateQuotationSchema` doesn't accept it), then a "Delivery" section — deliberately last, below Charges, not grouped with Coverage Period — with a single checkbox, pre-checked by default regardless of the quotation's own stored value (every revision defaults to notifying the client of the change), labeled "Email this revised quotation to the client" (distinct wording from QuotationCreator.jsx's own "Send this quotation to the customer's email" — a revision notice, not a first delivery).
- **SubmitQuotationDialog**: converts a quotation into a policy application via `POST /policy-quotations/:id/submit` — collects the payment fields a quotation never asked for, plus two checkboxes for the application's two independent delivery flags (both unchecked by default, matching `PolicyApplication.jsx`'s own wizard): "Email the customer now that the application is under approval (with payment instructions)" (`send_policy_to_email`) and "Email the customer their approved policy once this application is approved" (`send_policy_to_email_on_approval`) — neither is carried over from the quotation's own `send_policy_to_email`, since "email the client the quotation" and "email the client about the application it's about to become" are different decisions. Previews the resulting policy schedule (`previewApplicationPdf`, `isPreview: true`, application number shown as "TO BE ASSIGNED ON SUBMISSION") in a nested `Dialog`, and only then calls `submitQuotation`. Both this and EditQuotationDialog sanitize `null` DB fields (`vehicle.make`, etc.) to `""` before building preview props — `documentPreviewPropsSchema`'s vehicle fields are optional strings, which accept an omitted value but not an explicit `null`. Same checkbox-gated confirm as PolicyApplication.jsx's own preview dialog ("I have double-checked the information above and confirm it is correct." — Confirm and submit stays disabled until checked, reset every time the preview reopens) rather than a bare submit button — irreversibly converting a quotation deserves the same explicit double-check as filing a fresh application.
- **PolicyApplications** (Policy Applications tracker, `/policy-application`): same table/dialog shape as **Quotations** above — server-paginated table (`listApplications`) with a **Status column** (shared `components/StatusChip.jsx`, color keyed off `ApplicationStatus`) and a **Type column** (`policy_type` — a small locally-defined `NEW_POLICY`/`RENEWAL` label/color map, same "duplicate a small local mapping" precedent as `ClientPolicies.jsx`'s own `POLICY_STATUS_LABELS`; `PolicyApproval.jsx` keeps its own identical copy) that quotations don't have; row click opens the shared `components/ApplicationDetailDialog.jsx` (fetches the PDF via `downloadApplicationPdf`, renders via `PdfViewer`, "Re-export PDF"/`window.open` on the same blob, "Resend to client" via `resendApplicationEmail`); "New Application" opens `PolicyApplication` (the wizard, aliased on import as `PolicyApplicationCreator`) embedded in a `Dialog`. No Actions column — an application isn't edited or converted from here the way a quotation is. Also reads an `?open=<id>` query param on mount (via `useSearchParams`) — the Quotation Tracker's "For Issuance" link routes here with it, fetching that one application's detail (for the dialog title) and opening `ApplicationDetailDialog` straight away, then stripping the param back out of the URL (`replace: true`) so it doesn't re-trigger. A second, near-identical `?renew=<policyId>` param does the same for the Client Policies page's "Renew This Policy" action — fetches `getPolicyRenewalPrefill`, stores the result in `renewalPrefill` state, and opens the "New Application" `Dialog` (`setCreateOpen(true)`) with it passed straight through to `PolicyApplicationCreator` instead of opening a detail popup. `renewalPrefill` is reset to `null` both when the create dialog is closed and when "New Application" is clicked manually, so a later fresh filing in the same session never accidentally inherits a stale renewal's date floor.
- **ClientPolicies** (`/my-policies`, nav label "Client Policies", gated `VIEW_POLICIES`): an agent's own book of already-issued policies across all their clients — server-paginated table (`listMyPolicies`, own-agent-only, same scoping as `PolicyApplications`), columns Policy #/COC No./SA No./Insured/Class/Product Variant/Status/Effective/Expiry/Total Premium/Issued/**Actions**. `Status` uses a small locally-defined `PolicyStatus` label/color map (`ACTIVE`/`EXPIRED`/`CANCELLED`/`LAPSED`) rather than the shared `StatusChip.jsx`, since that component is specifically `ApplicationStatus`'s mapping and `PolicyStatus` is a different enum with different values — same "duplicate a small local mapping" precedent as the two dialogs' own `CHANGE_TYPE_LABELS`. Row click opens `components/PolicyDetailDialog.jsx`. The **Actions column** (`stopPropagation`ed so it doesn't also trigger the row's own detail dialog, same pattern as `Quotations.jsx`'s own Actions column) has two icon buttons: "Resend to client" (`resendPolicyEmail`, a top-level `Alert` rather than per-row feedback, since only one resend realistically runs at a time from this table) and "Renew This Policy" (`navigate` to `/policy-application?renew=<policyId>` — see `PolicyApplications.jsx`'s own `?renew=` handling and `PolicyApplication.jsx`'s `renewalPrefill` prop below). No create/edit affordance for a `Policy` itself here — one is only ever created via `POST /policy-approval/:id/approve`, never directly; "Renew This Policy" creates a new `PolicyApplication` (`policy_type: RENEWAL`), not a new `Policy`.
- **PolicyApproval** (`/policy-approval`, gated on `APPROVE_APPLICATION` rather than `CREATE_APPLICATION`): the cross-agent approval queue — same table shape as PolicyApplications (Type column included), plus an **Agent column** (`agent_name`/`agent_code`); no Actions column. Backed by `listApplicationsForApproval` (`GET /policy-approval`, every application in the system, not just the caller's own). Row click opens `components/ApplicationReviewDialog.jsx` — one wide dialog with the PDF on the left and the change-history/"Create Change"/"Approve" panel on the right, side by side, rather than two separate popups (an older version opened `ApplicationDetailDialog` for the PDF and a dedicated Actions-column button for a separate `ApplicationApprovalDialog` — both replaced by this single dialog). `onApproved` calls `loadApplications()` again so an approval's status change shows up in the table immediately.
- **PolicyApplication** (the wizard component, no longer a standalone route — see **PolicyApplications** above, same relationship as `QuotationCreator`/`Quotations`): linear wizard (each section only renders once the prior one is "complete") — Insured Party (individual/company, `Autocomplete freeSolo` search-and-reuse) → Insured Address → Insurance Class → Vehicle(s)/Risk Address (each vehicle's fields now include a required "No. of seats", threaded through `emptyVehicle`/`isVehicleComplete`/the plate-lookup-and-reuse flows/`VehicleEditDialog` the same way every other vehicle field already was) → Product Variant → Coverage Period & Coverages → Payment & Delivery → Charges. `resolveCoverageSelection()` mirrors backend pricing client-side (non-authoritative, server always recomputes). Payment & Delivery carries two independent checkboxes — "Email the customer now that the application is under approval (with payment instructions)" (`send_policy_to_email`) and "Email the customer their approved policy once this application is approved" (`send_policy_to_email_on_approval`). Preview `Dialog` gates the real submit: opening it fires a `useEffect` (keyed on `previewOpen`, not on the props themselves — the form behind a modal can't change while it's open, so this fetches once, not on every keystroke) that calls `previewApplicationPdf` and renders the response inline via `PdfViewer`; "Print / Save as PDF" reuses that same blob. `previewProps` also carries each coverage's `pricing_mode` (from the catalog) and `deductibleRate`/`authorizedRepairLimitRate` (from the selected product variant) — see backend's "PDF generation" section for what these drive. Submit disabled without `CREATE_APPLICATION.AGENT_ISSUANCE`. Accepts optional `onClose`/`onCreated` props, same pattern as `QuotationCreator` below (close `IconButton` when embedded, `onCreated(application)` after a successful save so the tracker refreshes without closing the dialog).
  - **`renewalPrefill` prop** (optional — see `PolicyApplications.jsx`'s `?renew=` handling above): pre-fills the whole wizard from an already-issued `Policy`'s own live records (`routes/policies.js`'s `GET /:id/renewal-prefill`), applied by one `useEffect` keyed on `[renewalPrefill, loading]` once the catalog/party lists are loaded — sets `insuredType`/`newCustomer`/`newCompany` (matched against the just-loaded `myCustomers`/`myCompanies` by id, the exact same shape the Autocomplete's own `onChange` populates), `classId`/`variantId`, `insuredAddress`/`riskAddress`, `vehicles`, `coveragePeriodDays` (a starting guess — the source policy's own effective-to-expiry length), and `coverageSelections` (`vehicle_indices: null` for every one — a `PolicyCoverage` row doesn't retain which vehicle it was originally scoped to, so this can only ever restart as "applies to all vehicles"; the agent re-narrows it same as any other application). Every value is genuinely live and re-editable from there — coverages re-price against current net rates the moment the effect sets them, not frozen figures copied from the old policy. A `skipPartyResetRef` guard suppresses the separate "reset vehicles/addresses whenever the selected party changes" effect for the one render this triggers (setting `existing_customer_id` here would otherwise immediately trip that effect and wipe out the vehicles/addresses this same prefill just set); `appliedRenewalPrefillRef` keeps the whole thing from re-running later and clobbering an in-progress filing (deliberately not depending on `myCustomers`/`myCompanies`, since `loadParties()` re-runs after every successful submit and would otherwise re-trigger this). `renewedPolicyId` is sent as `renewed_policy_id` in the create payload; `minCoverageStartAt` (the source policy's own `expiry_date`) both floors the "Insured from" field's native `min` and is checked again in `handlePreview` (a native `datetime-local` `min` isn't reliably enforced across browsers for a typed-in value) — an info `Alert` at the top of the form and inline `helperText` on the field both name the policy being renewed and its expiry constraint. Both are reset back to blank after a successful submit, since a renewal's date floor is specific to that one filing, not to "another one for the same party" afterward.
- **QuotationCreator**: near-identical twin of `PolicyApplication` minus payment fields and the `AGENT_ISSUANCE` gate; calls `createPolicyQuotation`; preview watermarked as a quotation. No longer a standalone route — rendered directly inside `Quotations.jsx`'s "New Quotation" `Dialog`. Accepts optional `onClose`/`onCreated` props: when passed (i.e. embedded), shows a close `IconButton` next to its own title and calls `onCreated(quotation)` after a successful save so the tracker can refresh its list without closing the dialog (the create flow resets and stays open, same as it always has, for filing another one for the same party). With `QUOTATION_TRACKER.ADMIN_CREATE_QUOTATION` (`canFileForOtherAgent`), a "Filing Agent" `Autocomplete` appears above "Insured Party" (options from `listAgentsForQuotation`, defaulting to the caller's own agent once `useAuth()`'s `agent` resolves) — picking a different one re-fetches the party lists from `listCustomersByAgent`/`listCompaniesByAgent` instead of `listMyCustomers`/`listMyCompanies`, resets the in-progress party selection (a reused customer/company/vehicle/address only makes sense for the agent it came from), and adds `agent_id` to the create payload (omitted — files under the caller's own agent, same as before this existed — whenever the picked agent is the caller's own). Without this permission the picker never renders and every party list/submission stays exactly as it was: the caller's own agent, unconditionally.

### Components (`src/components/*.jsx`)

| Component | Purpose |
|---|---|
| `ApplicationDetailDialog.jsx` | The application detail popup (fetches its PDF, renders via `PdfViewer`, "Re-export PDF") — used by `PolicyApplications.jsx` (an agent's own tracker, `showResend` defaulted `true`) only; `PolicyApproval.jsx` used to reuse it too (`showResend={false}`) but now opens `ApplicationReviewDialog.jsx` instead, which fetches its own PDF inline rather than composing this component (same "duplicate the fetch, don't share the dialog" precedent as `Quotations.jsx`'s own `QuotationDetailDialog`). Also fetches `listMyApplicationChanges` and lists every recorded `PolicyApplicationChange` in a read-only "Recorded Changes" table below the PDF (same `CHANGE_TYPE_LABELS` mapping as `ApplicationReviewDialog.jsx`, duplicated locally for the same reason) — an agent can see what an approver corrected on their filing, just without the "Create Change"/"Approve" actions, which stay approver-only in `ApplicationReviewDialog.jsx`. |
| `ApplicationReviewDialog.jsx` | `PolicyApproval.jsx`'s row-click popup — one wide `Dialog` (`maxWidth="xl"`) with the application's PDF on the left (own `downloadApplicationForApprovalPdf` fetch + "Re-export PDF", no resend: an approver reviewing someone else's application has no business emailing the customer on the agent's behalf) and, side by side on the right, the change-review panel: fetches `getApplicationForApproval` + `listApplicationChanges`, lists every recorded `PolicyApplicationChange` in a table, and has a "Create Change" inline form (`change_type` `Select` restricted to the 13-value `ApplicationChangeType` enum; conditionally shows a vehicle or coverage `Select` per type) that posts to `createApplicationChange`. The PDF-fetch effect is keyed on a `pdfReloadKey` counter bumped after a successful "Save Change" — so the PDF panel reflects the correction immediately, without closing and reopening the dialog. `INSURED_NAME_DETAILS`/`INSURED_ADDRESS_DETAILS` skip the generic "Current value"/"New value" text box in favor of separate, individually-prefilled inputs — Last/First/Middle name (or Company name for a `CORPORATE` application) for the former, every `updateAddressSchema` field (line 1/2, barangay, city, province, postal code, country) for the latter — composed into `new_value` via a local `formatInsuredName()` (mirrors the same-named backend helper) or sent structured as `new_address`, respectively, rather than asking an approver to retype one whole combined string from scratch. Right above the confirm checkbox, two optional text fields — "COC Number (optional)"/"SA Number (optional)" — let the approver attach either at the moment of approval; both are sent (trimmed, blank → `undefined`) in `approveApplication`'s payload and print on the issued Policy PDF (see backend's `pdf/policyPdf.js`). "Approve" (checkbox-gated, same confirm pattern as `SubmitQuotationDialog`) posts to `approveApplication` and shows the issued `policy_number` on success; both the change form and the checkbox disappear once the application is `APPROVED` (checked via `detail.status` or a local `approvedPolicy` flag, so it locks immediately after approving without waiting on a refetch). The two columns stack vertically below `md` width rather than side by side. Replaces what used to be two separate dialogs (`ApplicationDetailDialog` for the PDF, plus a dedicated Actions-column button opening its own `ApplicationApprovalDialog` for the review form) — an approver no longer has to close one popup to open the other. |
| `BrandMark.jsx` | Bethel logo/wordmark, two sizes. |
| `CoverageSelector.jsx` | Cascading class→variant→coverage `select`s; reused in `EditClauses`, `ManageCoveragePricing`. |
| `NumberField.jsx` | Thousands-separated display, raw string via `onChange`, preserves in-progress decimal typing. Used everywhere for money/number entry. |
| `StatusChip.jsx` | `ApplicationStatus` → label/color `Chip` mapping, shared by `PolicyApplications.jsx` and `PolicyApproval.jsx`. |
| `PermissionChecklist.jsx` | Grouped `Checkbox` list (via `utils/permissionGroups.js`); used in `ManageUsers`, `RoleDefaultPermissions`, `CreateRole`. |
| `PlaceholderPage.jsx` | Generic "Coming soon." stub. |
| `PolicyDetailDialog.jsx` | `ClientPolicies.jsx`'s row-detail popup — much simpler than `ApplicationDetailDialog`/`ApplicationReviewDialog` since an issued `Policy` has no change history and no resend-to-client action: just fetches and shows its own final, signed PDF (`downloadPolicyPdf` → `pdf/policyPdf.js` on the backend) via `PdfViewer`, plus a "Print / Save as PDF" button reusing that same blob. |
| `PdfViewer.jsx` | Renders a PDF inline given a blob object URL — `{ url, loading, error, height }`. The backend's PDF endpoints require an `Authorization` header, which a plain `<iframe src="...">` can't attach, so every caller `fetch()`s the PDF as a `Blob` (via `requestBlob` in `api/client.js`) and passes `URL.createObjectURL(blob)` in here. Renders each page onto its own `<canvas>` via `pdfjs-dist` (page fetched/scaled to the container's width, capped at 1.5x to avoid blurry upscaling) rather than embedding the browser's native PDF plugin in an `<iframe>` — deliberately not cosmetic: on mobile Chrome, that plugin running inside a nested `<iframe>` frequently doesn't accept touch-drag scrolling at all (mouse-wheel/scrollbar only, which a touchscreen has neither of), and there's no CSS fix for it. A canvas rendered directly into this component's own DOM has no nested browsing context to fight with, so it scrolls like any other page content, touch included. `GlobalWorkerOptions.workerSrc` is set from `pdfjs-dist/build/pdf.worker.min.mjs?url` (Vite's asset-URL import). Used by `Quotations`, `PolicyApplications`, `PolicyApproval`, `ClientPolicies`, `PolicyApplication`, `QuotationCreator`, `EditQuotationDialog`, `SubmitQuotationDialog` (via the shared `ApplicationDetailDialog`/`QuotationDetailDialog`/`PolicyDetailDialog`) — see "Printing/PDF" below. |
| `ProtectedRoute.jsx` / `RequirePermission.jsx` | Auth / permission route guards, see above. |

### Layout / Context / Nav / Utils

- **`layouts/AppLayout.jsx`**: `AppBar` + responsive sidebar (custom fixed `Box`, not MUI permanent `Drawer` — an inline comment notes that variant fought dynamic width overriding) built from `navItems`, filtered by permission.
- **`context/AuthContext.jsx`**: `token`/`user` persisted to `localStorage`; `permissions`/`agent` re-fetched via `getMe` on token change, not persisted.
- **`nav/navItems.js`**: static sidebar item list, doubles as the page-access-permission map.
- **`utils/coveragePeriods.js`** → `formatPeriodLabel(days)`.
- **`utils/currency.js`** → `formatPHP`, `formatRate`.
- **`utils/permissionGroups.js`** → `groupPermissions`.
- **`utils/vehicleValue.js`** → client-side mirror of the backend depreciation calc (non-authoritative, live-preview only).

### Shared UI conventions

- **Loading**: `loading` boolean + centered `CircularProgress`. No global spinner system.
- **Feedback**: local `error`/`success` string state → inline MUI `Alert`. **No Snackbar/toast anywhere in the app.**
- **Forms**: plain controlled state, no form library (no Formik/RHF); validation is HTML5 `required` + manual pre-submit checks, not a client-side schema validator.
- **Dirty-tracking**: settings-style pages keep current + original state, `isDirty` via `useMemo`, Save/Cancel only when dirty.
- **Dialogs**: `Dialog`/`DialogTitle`/`DialogContent`/`DialogActions` for all create/edit forms. Recurring "invite link" sub-pattern: dialog swaps to a read-only shareable-link view on success instead of closing, when email isn't actually sent.
- **Tables**: MUI `Table`+`Paper`, `Chip`s for status/tags. `Quotations.jsx` and `PolicyApplications.jsx` are server-side paginated (`TablePagination`); others load everything at once.
- **Printing/PDF**: there is exactly one implementation of each document's layout — the corresponding PDFKit builder in `backend/src/pdf/` — used for the on-screen preview, the download, and the email attachment alike. No `window.print()`/`#print-root` portal (removed; `index.html` has no print CSS), and no separate HTML preview component either (an earlier version of this had both `PolicySchedulePreview.jsx`, an HTML/CSS mockup, *and* the PDFKit files, which meant editing a layout in two different rendering systems to keep them in sync — that component was deleted once the dialogs switched to embedding the real PDF instead of a lookalike). Every preview/export flow follows the same shape: `fetch()` the PDF as a `Blob` (a `previewXPdf`/`downloadXPdf` function in `api/client.js`, via `requestBlob` — needed because these endpoints require an `Authorization` header, which a plain `<iframe src="...">` can't attach), `URL.createObjectURL()` it, hand the resulting URL to `components/PdfViewer.jsx` for inline display (rendered via `pdfjs-dist` onto `<canvas>` — see PdfViewer.jsx's own entry above for why not a native-plugin `<iframe>`), and reuse that *same* URL for the "Print / Save as PDF"/"Re-export PDF" button (`window.open(url, "_blank")` — synchronous, since the blob is already in hand by the time the button is clickable, so there's no popup-blocker timing issue to work around and no second network request).
- **Wizard/linear-disclosure**: `PolicyApplication`/`QuotationCreator` reveal each section only once the prior section's completeness check passes.

---

## Local dev DB reference

`backend/.env` has `DATABASE_URL`, `PORT`, `JWT_SECRET`, `JWT_EXPIRES_IN`, `FRONTEND_URL`, and commented `SMTP_HOST/PORT/USER/PASS/FROM` placeholders (fill in to make quotation "Resend to client" actually send). No `.env.example` file exists.
